# 人工智能 OS · v2 设计规范（DESIGN.md）

> 本文件是 ai-os v2 的**设定与规范唯一真源**：容器适配、导航结构、视觉语言、布局、兼容基线、工程与分期都以本文为准。实现（`_dev/build.py` 单源）与验收都必须对照本文。
> 容器与兼容细则引用仓库内 Skill：`.skill/minitool-zip-builder/`（SKILL.md 及其 references），本文不重复抄录，只落本项目的设计决策。

---

## 0. 背景：v1 的问题与 v2 的目标

v1（当前根目录构建产物，React + Tailwind 产物归档）实测（390×844 移动视口）暴露四类问题：

1. **不像真机**：深紫近黑的"霓虹雾"背景 + 满屏文字辉光，是"赛博面板"而非"手机"；缺少真机的状态栏、壁纸、Dock 质感与应用切换动效。
2. **顶部与宿主冲突**：v1 用 `base.css` 里 `left:35px` 硬偏移返回按钮去避让小红书容器自带按钮，属脆弱 hack；且各页返回钮样式不统一。
3. **纵向留白失控**：主屏第二行图标到 Dock 之间约 1/3 屏空洞；计算器键盘不贴底；助手页单条气泡下整屏空白。
4. **无源码**：v1 仅构建产物，无法可持续地改结构；v2 必须建立单源工程。

v2 目标：**一眼是一部真手机**（浅色壁纸 + 状态栏 + 图标网格 + Dock + 底部三大金刚键），同时满足小红书小工具容器与 Chrome 61 兼容基线。

---

## 1. 版本与归档策略

- v1 原样保留，整体移入 `archive/v1/`（`git mv`，可追溯），**不做任何修改**。
- 根目录成为 v2 源码工程，目录约定对齐 `perler-bead-game`：
  - `_dev/build.py` —— **唯一真源**，产出根目录 `index.html`（+ 内联或同目录 `main.js`/`style.css`）。
  - `_dev/build_zip.py` —— 校验并打包小红书规范 zip。
  - `_dev/smoke_test.py` —— 无头自检（导航栈、安全区变量、Chrome61 语法扫描）。
- 交付物（zip）与源码分离；zip 不入仓库或入 `dist/` 且 gitignore 由用户决定（默认不入）。

---

## 2. 容器适配规范（顶部留白 / 左右净空 / 底部金刚键）

参照 `perler-bead-game` 的 `--top-gap` 机制与 `.skill/minitool-zip-builder/references/cross-platform-h5.md §3`：

```css
:root{
  /* 顶部强制留白：小红书 iframe 内 env(safe-area-inset-top) 恒为 0，
     必须用显式常量兜底，保证顶栏不顶到屏幕最上沿。改这一个值即可全局调整。 */
  --top-gap:50px;
  --safe-top:calc(var(--top-gap) + var(--safe-area-inset-top, env(safe-area-inset-top, 0px)));
  --safe-bottom:var(--safe-area-inset-bottom, env(safe-area-inset-bottom, 0px));
  --safe-l:0px;   /* 恒为 0：宿主按钮只在顶部导航行，内容区 / Dock 无侧边按钮 */
  --safe-r:0px;
}
/* 宿主内嵌：左右净空同样为 0（不再让内容区 / Dock / 页脚内缩）；
   预留带加高到 70px —— 宿主顶栏（返回/分享/更多）就压在这条带里，
   50px 会被它占掉大半，带子下方只剩十几像素，看起来像"顶部没留白"。
   --safe-top 必须在 body 上重新声明：自定义属性在声明它的元素上求值，
   只覆盖 --top-gap 不会让 :root 上的 --safe-top 重算。 */
body.in-app{
  --safe-l:0px; --safe-r:0px;
  --top-gap:70px;
  --safe-top:calc(var(--top-gap) + var(--safe-area-inset-top, env(safe-area-inset-top, 0px)));
}
```

硬规则：

- **宿主按钮只占顶部导航行**：小红书自带按钮（返回 / 分享 / 更多）只出现在顶部一行，用 `--safe-top` 预留带**纵向**让开即可。内容区、Dock、页脚、金刚键栏**一律不做水平内缩**。
- **顶部预留带**（高度 = `--safe-top`）内：**禁止任何可交互元素**（按钮/链接/手势区）。in-app 时自绘状态栏隐藏（§4.1），该带即为纯留白，供宿主顶部按钮浮在其上。
- **禁止顶部返回按钮**。一切"返回/主页/多任务"由**底部三大金刚键**承担（§3）。v1 的 `left:35px` 偏移 hack 废除。
- 所有贴边栏（状态栏、Dock、金刚键栏、页内工具条）的垂直内边距用 `--safe-top` / `--safe-bottom`；水平内边距用固定 `Npx`（`--safe-l`/`--safe-r` 恒为 0）。
- **预留带高度必须有普通值兜底**：`.statusbar` 写 `height:50px; height:var(--safe-top);`（in-app 写 `height:70px; height:var(--safe-top);`）。`--safe-top` 是链式 `calc(var() env())`，任一段失效会让 `height` 整条 computed-value 失效并回退 `auto`（≈24px），预留带塌陷、内容顶到宿主顶栏上 —— 这正是 `css-compatibility.md §4`「先保留普通值，再用变量」的要求。
- **in-app 与纯浏览器直开的布局必须一致**（唯一允许的差异是「隐藏自绘状态栏」，§4.1）。历史教训：曾把 48/92 净空套到所有贴边栏，导致半行组件被拆成两行、Dock 图标被压扁挤在一起、每个应用页左右各留大片空白 —— 容器里与 `index.html` 直开明显不一致。

---

## 3. 导航结构：底部三大金刚键 + 应用栈

- 屏幕最底部（`--safe-bottom` 之上）固定一条**系统导航栏**，内含三枚金刚键（Android 风格几何 glyph，纯 CSS/SVG 绘制，不引图标字体）：
  - **返回 ◁**：弹出应用栈顶；栈空时回主屏。
  - **主页 ○**：直接回主屏（清空 transient 视图，保留各应用状态）。
  - **多任务 ▢**：打开最近应用卡片轮播（横向滚动卡片，点击恢复、上滑/点 × 关闭）。
- 导航栏为半透明实色底（基线）+ `@supports` 增强毛玻璃；键触控区 ≥ 48×48px。
- **导航栏高度 = `52px` + `--safe-bottom`**，安全区加在 52px **之外**（`height:52px; height:calc(52px + var(--safe-bottom)); padding-bottom:var(--safe-bottom);`）。**禁止**只写 `height:52px` 再用 `padding-bottom` 让安全区——`box-sizing:border-box` 下那会把内容盒压成 `52 - 安全区`，48px 的键居中后向上溢出到栏顶之上、上方毫无空间（纯浏览器 `--safe-bottom=0` 时看不出，容器/真机一注入安全区就露馅）。
- **应用栈**：主屏 → 应用 → 应用内二级页，统一入栈；返回键逐层出栈。页内不再各自画返回钮。
- 多任务卡片记录 {应用 id, 快照标题, 进入时间}；不截图（容器内 canvas 快照成本高），用「应用图标 + 名称 + 主题色标题条 + 骨架内容块」（`.rc-bar` / `.rc-mini` / `.rc-sk`）拼出可辨认的缩略卡，**不得只留一块空渐变**。
- 多任务遮罩 `position:absolute` 于视图容器内，只铺内容区，**不得盖住底部金刚键**（多任务下金刚键必须仍可按）。
- **遮罩的关闭判据是「不在卡片内即关闭」，不得用白名单 target**：空态（`暂无最近应用` 的 `.rc-empty`）是 `flex:1 1 auto`、撑满整个遮罩，不在白名单里就会导致"清空全部任务后点遮罩关不掉"。卡片自有 click（恢复应用）、清理按钮已 `stopPropagation`，故只需排除 `.rc-card` / `.rc-clear`；返回键同样可关。
- 视图切换用 `opacity + transform:scale(.965)` 短过渡（`.view.entering`），不用单一淡入。

---

## 4. 视觉规范：像一部真手机

### 4.1 状态栏（真机布局，纯展示无交互）
- 采用真机排布：**时间居左，信号格 / WiFi / 电池% 居右**（`justify-content:space-between`）。约束只针对"可交互元素"，静态内容贴边不影响宿主按钮。
- 状态栏 padding 为 `14px`（左右净空 `--safe-l`/`--safe-r` 恒为 0，见 §2）；in-app 时自绘状态栏整体隐藏，不会与宿主自带状态栏 / 顶部按钮挤占。
- 字号：时间 14px/600，电量 11px；颜色随壁纸明暗取 `--status-ink`。
- 信号/WiFi/电池为纯 CSS/SVG 小图形；电池百分比为模拟值（固定 87%）。
- 状态栏内**不得出现任何按钮或手势区**。
- **宿主内嵌（`body.in-app`）时必须隐藏自绘状态栏**：小红书容器自带真实状态栏与顶部导航条（返回 / 分享 / 更多），本应用再画一层会在屏幕上叠出「两层状态栏」，且自绘状态栏与宿主顶部按钮同处一行、互相挤占。用 `visibility:hidden` 隐藏（**不用 `display:none`**——那会让 `--safe-top` 预留带塌陷，内容区被顶到宿主顶栏按钮下面）；纯浏览器 / PC 模拟器无宿主状态栏时照常显示，真机观感靠它。

### 4.2 壁纸与主题（默认浅色）
- **默认浅色壁纸**：柔和网格渐变（如 `#EAF1FF → #FDF4EC` 的对角 mesh + 极淡噪点），深色文字 `#1C1B1F`。
- 提供 3–4 款壁纸（浅 mesh / 浅纯色 / 深 mesh / 深色纯），设置内切换，`localStorage` 持久化（key 前缀 `aios_`）；设置页 swatch 预览必须与实际壁纸**同色系**，不得另画一套配色。
- **壁纸只属于主屏**：进入任意应用后，状态栏区、内容区、底部导航一起铺**应用底色** `--app-bg`（浅 `#F3F3F7` / 深 `#121216`，导航用 `--app-nav`），壁纸完全让位——真机只有主屏见壁纸。实现由 `body.on-app` 单一 class 控制（`showOnly(node, isApp)` 切换），回主屏即移除。
- **深色模式**作为可选项（跟随壁纸 or 独立开关），但**默认浅色**；所有组件 token 化，切换只换变量。
- 文字辉光**全面移除**（v1 的 glow 是"不像真机"的主因之一）；仅保留图标自身的彩色渐变与投影。

### 4.3 主屏
- 壁纸铺满；图标网格 4 列（390px 宽下每格 ≥ 72px 触控区），图标为圆角方形（squircle 观感：`border-radius:23%`）彩色渐变 + 白色 glyph + 底部 12px 名称（400 字重）。
- 主屏图标**色相必须拉开**：计算器靛蓝 / 助手天蓝 / 日历橙 / 日程绿 / 闹钟紫 / 统计青 / **应用商店青柠（黄绿 hue≈92，其余图标都不在这个色区）** / 电话绿 / 短信深蓝 / 相机玫红 / 设置灰——不得出现两个近似橙或近似绿。
- **副标题/ slogan 不上主屏**（v1 的两行折行孤字问题直接取消）；slogan 移入"关于本机/应用详情"。
- 网格从顶部往下排（`align-content:start`、`flex:0 0 auto`），像真机启动器；最后一行之后的空白留在 Dock 上方（真机少应用时亦然），**不得让网格在剩余空间里垂直居中**（那会让图标"悬空"）。主屏整体（组件+网格）在 `.home-body` 内可滚动，小屏不裁切。
- Dock：半透明白（浅壁纸）/ 半透明黑（深壁纸）圆角条，**圆角独立取 28px**（明显大于普通卡片），4 枚常用应用**与主屏图标同尺寸（62px）**、`justify-content:space-between` 等距铺开、**不带文字标签**（真机 Dock 不带标签）；与底部金刚键栏之间留出 **≥24px 外边距**（容器视口比纯浏览器矮，主屏剩余空间被吃满时只剩这条外边距，10px 时 Dock 看起来是"贴在"键栏上）。

#### 4.3.1 小组件（首页丰富度与设计感）
组件区位于图标网格之上、Dock 之外。三种质感拉开层次：**主视觉渐变卡（时钟）→ 玻璃卡（天气）→ 银行卡质感深色卡（钱包）**；玻璃卡为 `.widget`（radius 20px，`@supports` 毛玻璃增强），渐变卡用 `.widget.w-xxx`（特异性 0,2,0）压过玻璃增强层。
- **时钟组件**（整行·品牌紫渐变卡）：内边距 20/18px 撑出英雄卡高度；白字 46px/800 大时间 + 11px 副行「M月D日 星期X · 本地时间，大概准」；右侧 60px 白环模拟小表盘（时/分针 `tick()` 按角度 `rotate()`，金色轴点）。**点击进闹钟**。
- **天气组件**（半行·玻璃卡）：**一眼假的设定数据**（恶搞内核）：「月球 · 静海」+ 26px 「-173°」与月牙 glyph 圆 chip 同行、「晴 · 流星雨概率 40%」、10px 免责行「数据来源：AI 编的，别当真」。**点按进入「月球天气」应用**（§4.9）——组件本身仍不联网（离线静态数据），它只是应用入口。
- **钱包组件**（半行·深色银行卡）：顶行「灵光银行 · 数字卡」+ 金色芯片；「余额（透支中）」+ 25px 假余额「￥ -99,999」；底行「今日支出 ￥9,999 / **** 2333」。**窄卡内数字一律 `white-space:nowrap`，不得折行**（字号按半行卡可用宽度定，宁可缩小也不允许"￥"独占一行）。**默认打码**（`￥ -****`），**点按显示**假余额 `￥ -99,999`、再点回打码；状态持久化 `aios_wmask`（缺省值 `1`，即首次进入也是打码态——余额按默认隐藏设计，不主动露富/露穷）。
- 半行组件用 `.w-half + .w-half{margin-left:10px}` 形成间距（Flex margin 基线，不用 gap）。
- 组件内交互仅限组件自身语义；不得在组件里放全局导航。
- 图标 glyph 一律手绘简单几何（矩形/圆/环组合），禁止凭记忆写复杂单 path（v2 初版齿轮曾糊成一团）。

### 4.4 应用内页面（三段式，杜绝留白失控）
统一骨架：`状态栏(固定) + 页头(标题居中, 无返回钮) + 内容区(flex-1, 可滚动) + 页内操作区(贴内容区底) `，整屏高度 = `100% - 金刚键栏 - safe`。
- 内容区用 `flex:1; min-height:0; overflow-y:auto`（基线写法），保证"内容撑满、操作贴底"。
- **禁止中段空洞**：内容少时不得出现"顶部堆叠 → 中段大片空白 → 操作区贴底"。显示类应用（计算器）把主显示卡设为 `flex:1` 并由其内部 `justify-content:flex-end` 吃掉剩余高度；列表/聊天类由列表或消息流 `flex:1` 撑满。
- 游戏页的操作键盘/输入区**锚定内容区底部**，不再悬空。
- **页脚操作区统一控件语言**：贴底操作区里的按钮一律**等分满宽**（`grid` 等分或 `flex:1`）、高度 48–64px、圆角 `--r-md`、双层投影 `--sh-1 + --hl`；主操作/选中态复用主 CTA 渐变（同 `.key.eq`/`.ring-btn`）。**禁止**靠左排列、按文字自适应宽度的小胶囊——它与键盘、判定按钮不成一体，会显得违和。
- **计算器页是固定页（内容区滚动的唯一例外）**：真机计算器不翻页。`.pbody.calc-body` 关掉内容区滚动（`overflow:hidden` 且不参与压缩 `flex-shrink:0`），显示卡 `.calc-display` 不压缩（表达式/大数不裁切、按键不跳动），净高不足全部由键盘行高消化（`.keypad` 行高 `minmax(0,64px)` 上限，`.pfoot.calc-foot` 可纵向压缩）——任何净高下都不得出现滚动条；净高 ≤700px 时指标条/显示卡紧凑化（`@media (max-height:700px)`），把高度让给键盘。判定阶段键盘**原位**隐藏（`visibility:hidden`）而非 `display:none`，「对 / 错」绝对定位覆盖在键盘区，页脚高度全程不变。覆盖层左右复用 `.pfoot` 的 `--foot-px-l/-r` 内缩量与键盘**同宽对齐，不得贴屏边**；提示行 `.calc-feedback` 是“怎么玩 / 现在要做什么”的关键指令，常驻可读（14px `--ink`，不被按键清空），`ask/ok/no` 三态分别取 accent/ok/danger 加粗，行高恒定 20px（切换状态不改变高度）。
- 空态必须有引导文案或示例 chips（推广 v1 日历页"点击任意日期开始排雷"的好实践）。

### 4.5 组件与色彩 token
- 语义色一套全 App 复用：`--accent`(品牌紫) / `--ok` / `--warn` / `--danger` / `--combo`(连击橙)；得分=accent、连击=combo、中性=前景色，**不再随意着色**。
- 圆角两档：卡片 16px、控件 10px；描边 1px `rgba(0,0,0,.06)`（浅）/`rgba(255,255,255,.08)`（深）。
- 顶栏指标条：主指标（分数）放大突出，次要指标降级为小字一行；≥5 个指标时折叠。
- 字体用系统栈（`-apple-system, "PingFang SC", "Microsoft YaHei", sans-serif`），不引 webfont（性能与兼容）。

### 4.6 质感系统（去廉价感，v2.1 增补）
廉价感五根源：单层硬投影、灰玻璃+灰描边一刀切、糖果随机渐变、字重字距无层级、壁纸平涂无颗粒。对策全部落在 token：
- **圆角三档** `--r-lg:24 / --r-md:18 / --r-sm:12`；组件用 lg，卡片 md，控件 sm；**Dock 独立取 28px**（真机 Dock 圆角明显大于普通卡）。
- **双层投影** `--sh-1`(环境 0 1px 2px) + `--sh-2`(主光 0 10px 28px)；深色模式整体加重。图标用**中性主光** `0 6px 14px rgba(20,20,40,.18)`（深色 `rgba(0,0,0,.52)`）——真机图标不用同色彩色光晕，彩色光晕显廉价，`--icon-sh` 已废除。
- **内高光/内底影** `--hl: inset 0 1px 0 rgba(255,255,255,.55)`（深色 .08）给材质厚度；图标加 `inset 0 1px 0 rgba(255,255,255,.35)` + `inset 0 -1px 0 rgba(0,0,0,.10)`。
- **发丝描边** `--hair`：浅色 rgba(255,255,255,.6)、深色 rgba(255,255,255,.10)，替代灰 line 用于玻璃面。
- **颗粒** `--grain`：feTurbulence SVG data-URI（opacity .045）作为每张壁纸的第一背景层，消除平涂塑料感；渐变卡另叠顶部 radial 高光纹理。
- **排版层级**：英雄数字 800 字重 + `letter-spacing:-1px` + `font-variant-numeric:tabular-nums`；标签 10–11px + `.3px` 字距；应用名 500 字重。
- **微动效**：`.app-tile:active{transform:scale(.94)}` + `transition:transform .16s ease`（transform/opacity 均在 Chrome 61 基线内）。

### 4.7 游戏玩法（二期a：计算器 / 闹钟 / 日历 —— 以 v1 逆向为准，禁止臆改）
三个游戏统一遵循 §4.4 三段式（指标条 + 主显示卡 + 贴底操作区），**机制规则以 v1 构建产物逆向为唯一真源**（v2 初版曾按文案臆造机制，已回炉）。指标条主指标 accent 色、连击 combo 色，数字一律 `tabular-nums`；分数/最高分等跨会话持久化于 `localStorage aios_stats`。
- **计算器（v1 规则）**：用户在键盘上自己按表达式（`+ − × ÷ . C =`，递归下降解析、一元负号、除零/非法显示 Error）；按 `=` 后 **50% 概率注入错误**（四种策略：±1~2 / 替换一位数字 / 交换相邻数字 / 量级偏移；偏差超 `max(30%,5)` 兜底小偏移，保留 2 位小数），随后进入判定阶段（键盘**原位**隐藏、出现「对 / 错」，页脚不塌陷、页面不跳动）。判对 `+10`，连击 ≥3 再 `+5`；判错连击清零、`score-5`（下限 0）；轮次 +1、准确率与最高分滚动更新；判定反馈 1.2s/1.6s 后回到键盘。恶搞内核：它真会算错，考的是玩家验算。
- **闹钟（v1 规则）**：目标时间对齐到整 `:00`/`:30` 刻度（生成器移植 v1 `$a`）；100ms 心跳，**响铃窗口 ±500ms**，窗口外「响铃」按钮禁用，窗口开启按钮脉冲高亮；**错过窗口自动记一次「晚了」**，窗口内按下记「准时」。结果文案「你比我准时 / 你和我一样睡过啦？」+「再来一次」（retry 顺延到下一刻度）。指标：准时次数 / 尝试次数 / 准时率。**自动判晚的判定必须用 `delta > 500`，不得用 `alarmState.ringing` 当哨兵**——目标设下时就已过期的情况（如 `:00.6` 打开闹钟，目标对齐到已过的 `:00`）`ringing` 仍为 false，两条分支都不命中，会死锁成「按钮永远按不动、也不自动判晚」。**当前时间用模拟表盘呈现**：`.a-clock`（120px 圆形，复用主屏 `.w-analog` 的指针语言——`transform-origin:50% 100%` + `rotate()`，时针 `(h%12)*30+m*0.5`°、分针 `m*6`°，accent 中心轴点），表盘下方 `.alarm-now` 小字数字时间辅助读数。
- **日历（v1 规则）**：7 列棋盘；难度 简单 5 行 5 雷 / 中等 5 行 8 雷 / 困难 6 行 12 雷（默认中等），banner「本月由 AI 重新排期，共 N 天」。**整屏摆下、不上下翻页**：格子是正方形（`padding-top:100%`），`fitBoard()` 先按自然尺寸布局，内容区溢出时按溢出量反推应缩到的棋盘宽度（`.ms-week`/`.ms-grid` 同宽 + `margin:0 auto` 居中，`MS_GAP=6` 与 CSS 的 `grid-gap` 一致，迭代几次收敛、格子下限 14px），宽屏保持自然尺寸；难度切换 / 结算文案折行 / 旋屏（resize）后都要重算。**棋盘视觉与版式**（全部落在 §4.5/§4.6 既有 token 与控件语言内，不另造一套）：未开口面格=抬起块（`--cell-hi/--cell` 双色渐变 + 外发丝环 + 环境影 + 内高光，全走 `box-shadow` 所以不占布局、格子仍是正方形），已开口=凹陷平底（`--cell-rev` + 内影）；格子圆角取 **23%**（与主屏图标同一套 squircle 观感，随格子大小自适应）；星期行=浅底圆角日期条（六/日只做中性加重 `--ink`，**不占用语义色**，与棋盘同宽对齐）；提示文案沿用站内通知条形态（卡面 + 发丝描边 + `--r-md`，同 `.sms-banner`）；**底部难度选择=等分满宽的三段控件**（`grid-template-columns:repeat(3,1fr)` + `--r-md` + 双层投影，高度 48px，与 `.key`/`.judge-btn` 同一套控件语言），选中态复用主 CTA 渐变（同 `.key.eq`/`.ring-btn`），**不做散在左侧的小胶囊**；棋盘卡用 `margin:auto 0` 在内容区剩余空间里**垂直居中**，消除 §4.4 禁止的中段空洞；净高 ≤700px 时指标条/通知条/星期条/状态条/难度按钮紧凑化（`.cal-body` 作用域，难度按钮在页脚故用 `.ms-foot` 限定），把高度还给棋盘。**首点布雷且避开首点 3×3**（首点必安全）；flood-fill 揭空；**长按 400ms / 右键 / 标旗模式** 三种插旗方式；计时 1s 上限 999s。胜利得分 `base(20/35/55) + (用时<60s ? 10 : 0)`；胜率与轮次为**移动平均**（失败也 +1 轮次摊薄胜率）；最高分/最快纪录持久化。指标条：分数/最高分/胜率/最快；第二行：用时/剩余雷/标旗模式。方格用 `padding-top:100%` 比例盒（Chrome 61 基线，不用 aspect-ratio）。
- **统计存储键**：`calc{score,highScore,streak,maxStreak,rounds,accuracy}` / `alarm{onTime,attempts}` / `cal{score,highScore,winRate,rounds,fastest}`，供二期b 统计应用消费。
- 保留的 v2 改良（非机制）：未开局面格显示日期数字、v2 视觉 token、三段式布局、**计算器 Error 态后再输入自动清空重来**（v1 需手动按 `C`；Error 态显示卡描边转红，下一次输入即复位）。

### 4.8 其余应用（二期b：助手 / 日程 / 电话 / 短信 / 相机 / 统计 —— 同样以 v1 逆向为准）
- **助手**：聊天式问答。随机问候语（`AI_GREET`）→ 打字气泡 1.5s → 从 80 题常识库（`QUIZ`）抽题（`used` 去重、抽空重置），底部 2×2 选项；答对 `+10`、连击≥3 `+5`、答错清零 `-5`（下限 0），准确率移动平均；AI 用 `AI_RIGHT/AI_WRONG` 文案点评（答错时把 `{answer}` 换成正确项）。**聊天区贴底**：选项上屏会加高页脚、压矮内容区，`scrollBottom()` 必须在该布局变化后再调一次（并在下一拍补一次），否则最新一条会被页脚增量顶出可视区。
- **日程**：select→memorize→recall→result 四相。难度 3条/10s、5条/8s、8条/5s（`SCHED_DIFF`）；日程池 `SCHED_POOL`（带时间，按时间排序）随机抽 N 条记忆；回忆逐条四选一（正确项+其余记忆项+干扰池 `SCHED_DISTRACT` 补足打乱）；每条 `+5`、全对 `+10`，准确率=正确/总数 移动平均。**版式**（与日历/助手/闹钟统一页脚操作区语言，§4.4）：四相内容装进 `.sched-block`（`margin:auto 0` 在内容区剩余空间里**垂直居中**，溢出时 auto 归零、交内容区滚动，消除中段空洞）；**操作全部下移 `.pfoot`**——难度选择复用日历的 `.ms-foot`+`.diff-btn`（等分满宽三段控件，选中态主 CTA 渐变），回忆四选一复用助手的 `.opt-grid`+`.opt`（2×2 选项），结算用 `.act-grid`+`.act-btn`（两枚等分满宽，主 CTA 渐变 + 次级卡面，同键盘/判定按钮语言）；记忆相 pfoot 放 `.pfoot-hint` 提示（等待状态）；记忆列表收进 `.sched-list` 卡面、时间用 accent 胶囊；阶段标题 700/19px（800 字重是英雄数字专用，§4.6）；净高 ≤700px 时标题/倒计时/列表行紧凑化，8 条困难列表也能一屏看全。
- **电话**：拨号盘（`*#0-9`，最长 11 位，3-4-4 格式化）；拨打后 dialing 3–8s → connected 计时 → 5–15s 自动挂断，20% 概率 `callEnded2` 否则 `callEnded1`；记录存 `aios_phone_records`（最多 5 条，点击复拨）；通话中按钮变红色挂断态。**版式**：内容区（`.pbody.phone-body`）**整页不滚**（`overflow:hidden`），高度全部交给「最近通话」列表（`.hist-card` 内的 `.hist-scroll` 是**全页唯一滚动条**），严禁在 `.pbody` 与内层列表各挂一条滚动条；**拨打后收起拨号键盘**——进入 dialing/connected/ended 即在视图上挂 `in-call`，键盘（`.dial-pad`）与通话记录卡（`.hist-card`）一并隐藏，号码·状态·计时（`.phone-top`）在剩余空间里垂直居中，两侧按钮用 `visibility`（非 `display`）隐藏以保住三列网格，挂断键继续落在中列不左右跳；挂断/「完成」后复原。空文案元素（`.call-ended-msg:empty`）不占位。**挂断图元**用 Material `call_end`（朝下的 ∩ 形听筒），与绿色呼叫键的 Material `call` 同一套图标语言；不得再给这条本来就「已朝下」的路径叠 `rotate(135°)`（那会被转成一块斜碎块）——它比 `call` 宽扁、同 viewBox 下显小，故单独放大一档（`.call-btn.hangup svg` 24px）。**通话结束态**（`is-ended`）拨号行整行让位给**满宽「完成」主 CTA**（`.act-btn.primary`，同日程/日历结算动作的控件语言），不把两个字塞进 50px 圆钮——那既挤又与红色挂断的语义打架。
- **短信**：种子收件箱 `SMS_SEED` 5 条（横幅「AI 已为您智能分类——全部分到'不重要'文件夹」）；会话内可发送，AI 延迟 2s 从 `SMS_REPLY` 敷衍回复池随机应答；已读/未读点、消息持久化 `aios_sms_messages`。**版式**：会话行的「名称 + 摘要」必须各自 `display:block`（`.conv-name` / `.conv-prev`）——两者若留在行内，`text-overflow:ellipsis` 对行内盒无效（长摘要不截断、横向冲出屏幕），且两段文字共处一条行盒会紧跟彼此并压到右侧时间戳上；摘要恒定单行省略号截断、不压 `.conv-time`。气泡 `.bub` 加 `overflow-wrap:break-word`：用户可发出长串不可断行内容（URL / 连续英文数字），只在空格断行会把气泡撑破、文字溢出卡面。
- **相机**：**纯模拟**（v1 即无 getUserMedia）：取景框**轮播本地素材图**——**只有一套美食图**（36 张）由 `_dev/make_cam_photos.py` 从 `vibeknow/world-food-3d` 借材派生为 `./assets/cam/food-*.webp`（512×512、q80，共 36 张、约 0.95 MB，随 zip 分发），每 3s 随机换一张且**不与上一张重复**；`object-fit:cover` 铺满取景框（不拉伸变形），新图**解码完再换 `src`**（未就绪时保持上一张，取景框不闪白/留空）；解码引用只留最近 12 张（其余交 GC），长时间挂着相机不把整套素材全留在内存里。素材不外链、不联网，清单在构建时由 `build.py` 扫描 `./assets/cam/` 生成（不手抄文件名）。
- **拍完即换，不逗留**：**没有预览页、没有「重拍 / 保存」**——按快门只做三件事：闪光 150ms（⚡可关）、**立刻换下一张素材**、重置 3s 自动换景计时（刚拍的那张不会被瞬间顶掉）。快门按钮恒在页脚中列（左列空占位保住三列网格）。
- **镜头温控（温度是拍照频率的函数，不是随机数）**：基准 `TEMP_BASE 36.0℃`，每次快门 `+1.5℃`（上限 `TEMP_CAP = 基准 + 10`），静置按 `0.5℃/s` 线性回落到基准；散热用**真实时间差**结算（500ms 心跳 + 每次快门/进页前先结算），与定时器节拍漂移无关，离开应用期间也照常降温。`≥43.5℃` HUD 转红并改文案为「镜头过热 xx.x℃」；`≥45℃` **封锁快门**（`.cam-shutter.blocked` 转灰 + 徽标提示「太烫了，凉一下再拍」，跨过封锁线时另提示一次「镜头过热，先别拍」），凉回线下才能继续拍。封锁只挡拍摄、不停取景轮播。
- **统计**：canvas 六维雷达（计算力/知识量/记忆力/排雷力/守时度/连击力，v1 `Y0` 算法：连击力=`avg(maxStreak/rounds)`×100，排雷力=胜率）+ 综合评分（六维均值）+ 四卡（最高总分/总轮次/平均准确率/闹钟准时）+ 分游戏记录与重置（确认弹窗）。**页脚「重置全部数据」复用 `.act-btn.danger`（满宽 danger 渐变，同 `.ring-btn`/`.act-btn.primary` 控件语言）**；行内单游戏「重置」用淡红底色文字按钮（`.rec-reset`，非描边胶囊）。
- **数据与文案**：全部 v1 设定（80 题、日程池、干扰池、短信种子、敷衍回复池、通话/闹钟/日历文案、雷达标签）已从 `archive/v1` 提取进 `_dev/build.py` 单源（「v1 数据还原」块），恶搞文案为产品设定，不得改写成"貌似真实"。相机取景由 v1 的「随机渐变 + 圆/★/♥」改为**实际美食素材图轮播**（来源见上条），故 `CAM_GRAD` / `CAM_COLORS` 已移除。

### 4.9 月球天气（天气应用：唯一入口是主屏天气小组件）
把 `vibeknow/moon-3d` 的 3D 月球整合进 ai-os，改造为一台「月面天气站」。**入口只有主屏天气组件**（§4.3.1 点按）——不进 `APPS` / `DOCK`，主屏保持 7 图标 + 4 坞位（应用商店占掉第 7 格），多任务里以卡片形态出现；应用页仍是 §4.4 三段式。**整页只有天气**：不做分段切换、不做月相滑块、不做月面档案与显示设置。
- **渲染方式：直接采用 moon-3d 的 Three.js 实现**（本地 `./assets/three.min.js`，与 `vibeknow/earth-3d` 同一套引入方式，零 CDN）：
  - `WebGLRenderer`（`antialias`）+ `outputEncoding = sRGBEncoding` + `ACESFilmicToneMapping`（曝光 `1.1`）→ `Scene` / `PerspectiveCamera(45)`；
  - 月球 `SphereGeometry(5, 64, 64)` + `MeshStandardMaterial({ map: moon.jpg, roughness: .95, metalness: 0 })`；
  - 光照 `AmbientLight(0x223044, 0.4)` + `DirectionalLight(0xffffff, 2.4)`；**太阳方位角由真实月相给**：`sunLight.position.set(sin a·d, 0, −cos a·d)`（`d = 4R`，`a=180°` 光从相机方向来即满月，与 moon-3d 的 `setPhase` 同一套几何）；
  - 背景 `SphereGeometry(2500)` 星空天球（程序化 `CanvasTexture`，含银河带与星云色斑）+ 近层 2600 点 `Points` + 两层加法辉光 `Sprite`；
  - 相机球坐标 `theta/phi/radius`：拖动改 `theta/phi`（`phi` 限位 `0.1~π−0.1`），滚轮 / 双指捏合改 `radius`（`R_MIN 8 ~ R_MAX 60`）。
- **不自转**：**取消自动自转**（`moon.rotation.y` 只由手势驱动，恒为 0），观测台只在 `dirty` 时 `render()`；心跳只负责「每分钟对一次表」，视图隐藏即停帧（onShow / 缩放 / 贴图到货再踢一脚）。像素比按 moon-3d 口径封顶 `1.5`。
- **月相按当前时间推算**（不设滑块）：以 2000-01-06 18:14 UTC 那次新月为基准，按平均朔望月 `29.530588853` 天推月龄 → 日照角（0° 新月 / 90° 上弦 / 180° 满月 / 270° 下弦）、照亮比例 `(1−cos a)/2` 与观测建议；未做中心差修正，相对真实朔望误差在半天以内。
- **降级**：无 WebGL / 上下文丢失时观测台显示 `.wx-fallback` 提示（`data-gl="fail"`），**天气读数照常可用**，不留白、不崩页面。
- **资源与构建**：`assets/three.min.js` 与 `assets/moon.jpg` 随包分发（月面贴图为 moon-3d 原图）。**贴图受浏览器跨源规则约束**：容器 / 本地 http 打开时同源正常；`file://` 直开时 WebGL 会拒绝本地贴图（`data-tex="fail"`，月亮退化为素色球），故 `_dev/smoke_test.py` 以 `--allow-file-access-from-files` 启动 Chromium 让两条路行为一致。README 需提示「推荐用本地服务器 / 容器打开」。
- **读数（12 格）**：与主屏组件同源的英雄区（`-173°` / `晴 · 流星雨概率 40%`）+ 12 格：前四格**由时间推得**（月相 / 月龄 / 照亮比例 / 观测建议，`data-f` 供自检对拍），其后为月面温度、昼面温度、太阳风、大气压、辐射剂量、月尘静电、能见度、紫外线指数；页脚免责行「数据来源：AI 编的，别当真」。
- **数据与文案**：天气读数必须与主屏组件同源（不得另造一套）；月相命名（新月/蛾眉/上弦/盈凸/满月/亏凸/下弦/残月）沿用 moon-3d；恶搞内核（数据来源是 AI 编的）不得改写成"貌似真实"；月相与观测建议是真天文口径，不得为了好看看手改。

### 4.10 开机启动屏
全屏覆盖约 1.8s 开机动画（头像 + 进度条 + 分区文案）后淡出进入主屏，不可跳过；JS 就绪前默认显示，避免白屏。**启动头像使用用户小红书头像**（`./assets/avatar.png`，88×88、圆角 24%、`object-fit:cover`），底部标注 `Designed by 人工智能Ding🥕`。**与容器存储水合并行**，容器端能力慢也不拖长开机时间；主题必须等水合完成再套用（否则默认值会盖掉已保存的壁纸/深色）。

### 4.11 系统音效（统一音效层）
全系统只有**一张音效表 + 一条总线 + 一个开关**，覆盖导航、点击、输入、判定、快门、通话、消息等所有交互。

- **合成而非播放**：Web Audio 实时合成（振荡器 + 噪声 + 指数包络），**零音频文件、零外部资源**。小工具红线禁止外链，音频文件又得随包走（体积账不划算），故与 `air-tycoon` / `solar-voyager` 走同一条红线。无 Web Audio 时**整层静音降级**，不抛错、不阻塞任何交互。
- **命名即语义**：音效名是系统语义 —— `tap`（通用点击）/ `nav`（金刚键）/ `back` / `open` / `on` / `off` / `key` / `tick` / `eq` / `ok` / `wrong` / `deny` / `win` / `lose` / `boom` / `flip` / `flag` / `alarm` / `over` / `shot` / `sent` / `msg` / `call` / `connect` / `hangup`——不是「某个页面的音」：**同一语义在任意应用里听起来一致**，新页面接音效只需标一个名字，不必自己再搭一套。
- **触发靠事件委托**：文档级 `click` 委托 —— 可交互元素默认发 `tap`，个别元素用 `data-sfx="名字"` 指定语义；`data-sfx="none"` 表示该处由应用自己的动作发声（判定对错 / 快门 / 挂断 / 发送…），**避免两个声音叠在一起**。同一音效 30ms 内只发一次（去重连点与双触发）。`data-sfx` 必须标在最内层的可交互元素上（委托走到最近的 `BUTTON` 就停）。
- **首个手势前不建 AudioContext**：自动播放策略会拦住它、控制台还会留一条 warning；`pointerdown` / `keydown` 里打点之后才建上下文，故**开机启动音在被策略允许前不会出声**（第一声天然是第一次点击）。音量与时值在音效表里定死：连点型（`tap` / `key` / `flip`）一律 ≤50ms 且不带尾巴，长尾会在连点里攒成噪音。
- **可整体静音**：设置「声音与触感 → 系统音效」开关，落 `aios_sound`（`'1'` / `'0'`，缺省开，与其它键同走双通道存储）；关掉后全系统安静，开关自身发 `off` 确认音、开启时发 `on`。v1 的「声音」吐槽行**原样保留**（恶搞内核不改写），开启开关时顺手复用它做提示。
- **自检接缝**（只读）：`AIOS.soundEnabled()` / `AIOS.soundNames()` / `AIOS.soundStats()`（上下文状态、是否降级、各音效触发次数）—— 无头环境听不见声音，断言走「该出声的地方确实触发过」。

### 4.12 应用商店（介绍 ai-dev-kit 已开发的小工具）
套着手机外壳，店里的商品是**整个 ai-dev-kit 仓库已经做出来的小工具**——不是本机已装的 9 个应用。它给这台模拟机一个「来源」：这些应用打哪儿来。列表进 `APPS`（主屏第 7 格），色相青柠（§4.3）。

- **数据真源是仓库根 `TRACKS.md`，构建期派生**：`_dev/build.py` 的 `store_apps()` 解析分类表，把每项**折成商店口径**后注入 `main.js` 的 `__STORE_DATA__`（`ensure_ascii=False`，中文原样）：
  - **上屏**：`n` 名称、`tag` 副标题（`store_tagline()` 折出）、`st` 上架标签 + `tone` 语气色（`store_state()` 折出）；
  - **不上屏**：`d` 目录、`t` 定位原文、`s` 物料状态原文 —— 只随数据注入，供构建门禁与自检对拍。
  **禁止手抄清单**：手抄件在新增项目、改定位、改物料状态时必然与上游分叉，且没人会记得回来同步 —— 与小工具包体的「相机清单扫描目录注入」是同一条规矩。`TRACKS.md` 缺失直接拒绝构建；跳过 `.skill/`（打包器、录制器是技能，不是应用），表头 / 分隔行按「目录列不以 `/` 结尾」自然滤掉。
- **一律说商店语言，不说文档语言**（本节关键约束，也是返工教训：第一版把索引字段直接搬上屏，README 里的目录、验收日志一并露脸，整页读起来像它的 README）：
  - **不出现目录路径**：`vibetool/offwork-heatmap/` 这类地址是仓库索引的字段，商店里不摆。
  - **不出现物料状态原文**：原文混着验收记录、包体体积、投稿口径（air-tycoon 那条是整段验证日志）。卡片只给折算标签：`ok`=已上架（已发布 / 完整 / 可玩 / 已打包）、`warn`=开发中（待落地 / 未做 / 缺物料）、`dim`=内部自用。`store_state()` 是唯一折算处，`smoke_test.py` 独立复算同一规则对拍。
  - **副标题不是索引长句**：定位常带技术括注与上游路径（ai-os 那条点名了 `vibeknow/moon-3d` 与 `_dev/make_cam_photos.py`），`store_tagline()` 砍掉第一个「（」或「：」之后的展开，只留「它是什么」，长度交给一行省略号。
  - **店门口径**：店头「已收录 N 款 · ai-dev-kit 出品 · N 个分类 · 点卡片看简介」；本机（ai-os 自己）那一行标**「已安装」** —— 商店里「你现在正用着它」的说法（原「· 当前设备」是设备视角，不像商店）。
  - 所有文本（含 `TRACKS.md` 来的）一律经 `esc()` 转义再进 `innerHTML`。
- **纯展示，不做跳转也不做展开**：容器内不能打开别的项目（也禁止外链），故卡片不做「安装 / 打开」；**一行一卡、静态不展开** —— 副标题本身就是商店短句，完整定位回仓库索引（`TRACKS.md`）查，不在店里堆正文。
- **版式**（全部落在 §4.4 / §4.5 / §4.6 既有 token 与控件语言内，不另造一套）：页头「应用商店」→ **顶部固定带**（渐变店头 + 分类筛选，不随列表滚走：30 多个项目翻到中后段还要能切分类）→ 内容区（全页唯一滚动区）= 四组「分类标题 + 货架卡」。
  - 店头：**品牌渐变横幅**（与主屏时钟组件同一套「主视觉渐变卡」语言，§4.3.1）+ 购物袋水印，白字「已收录 N 款」（英雄数字 800 字重 `tabular-nums`，N 由注入数据算出，不写死）。
  - 分类筛选：**等分满宽五格**（`repeat(5,1fr)` + `--r-sm` + 双层投影，高 38px，与键盘 / 难度选择 / 判定按钮同一套控件语言），选中态复用主 CTA 渐变（同 `.key.eq` / `.diff-btn.sel`），不是散在左侧的小胶囊。
  - 货架卡 = **招牌图标 + 名称 + 副标题 + 上架标签**。图标 46px、squircle 23%，与主屏图标同一套中性投影与内高光；**颜色取「货架色板」按序轮转**（8 组身份色，同分类内相邻必不同色）—— 它是每个应用自己的招牌色（与主屏图标同一性质），属身份色而非 §4.5 的语义色，故可多色并列；这也正是「不像一段文档」的观感来源。只有上架标签用语义色（ok / warn / 中性），本机那枚用 accent。
  - 分类色（实用工具蓝 / 互动游戏紫 / 数字艺术玫红 / 人文知识青绿）只用于分组圆点（同 `.rec-mini` 的用法：内容标识，不是控件）。
- **自检接缝**（只读）：`AIOS.storeGroups()` 返回注入数据的只读副本（含不上屏的 `d` / `t` / `s`）；`_dev/smoke_test.py` 自带一份独立的 `TRACKS.md` 解析与两套折算规则的独立复算，与注入数据、上屏 DOM **三方对拍**（分类名与顺序、逐项 分类 + 名称 + 目录 + 定位 + 状态全量续存、副标题与上架标签的折算、卡片数 = 项目数、`.skill/` 零命中、**目录路径与状态原文零上屏**、招牌图标相邻不同色、本机标「已安装」、筛选计数、无 console 错误）；`_dev/build_zip.py` 再加一条门禁：逐项能在 `main.js` 里对上 `TRACKS.md`（改上游不重跑构建会被拒绝打包）。

---

## 5. 兼容基线（硬门禁）

对齐 `.skill/minitool-zip-builder` 的 `js-compatibility.md` / `css-compatibility.md` / `device-capabilities.md` / `performance-budget.md`：

- **JS**：交付代码 ES2017 / Chrome 61。`_dev/build.py` 产出即为最终代码，不写 `??`、`?.`、`class fields`、`async generators` 等超基线语法；用 `var`/`function` 或 ES2017 内能力。Web API 超基线先检测再增强。
- **CSS**：Chrome 61 基线层 + `@supports` 增强层，不维护两套样式表。
  - Flex 间距基线用子项 `margin`；`gap` 仅作增强（沿用 v1 `flex-gap-polyfill` 的行为检测思路，或干脆全用 margin 基线、不增强）。
  - 不用 `aspect-ratio` / `inset` / `min|max|clamp()` / 逻辑属性 / `dvh` 作为唯一实现；`backdrop-filter` 必有实色兜底。
  - 多行截断用 `-webkit-box` 套件并保证失效仍可用。
- **禁用能力**：不联网（无 fetch/外链/外字体）、无 Worker/SW、无 `window.open`/`prompt`、无剪贴板/ geolocation / 传感器、无 eval、无 iframe、无 blob 下载。相机用 `getUserMedia`（手势触发）或降级为模拟取景框。
- **存储**（对照容器能力清单 §2.4 / §3.6 / §3.7）：
  - **容器 Storage JS API 优先**：客户端 ≥ **9.46.0** 且 `window.xhs.miniTool.setStorage/getStorage` 可用时，走 `setStorage` / `getStorage` / `getStorageInfo`；`localStorage`（前缀 `aios_`）**只作低版本客户端的降级通道**。
  - **版本判断**：`buildVersion` 末 3 位是编译序号，比较前先 `Math.floor(buildVersion / 1000)`；`window.xhs` / `launchOptions` / `miniToolEnv` **逐级判空**，同步取不到再异步 `getLaunchOptions()` 兜底，两条路都失败按「不支持」处理。
  - **回调而非 Promise**：容器端能力统一走 `success` / `fail` 回调（Promise 版在旧容器上不可靠），并加超时兜底——容器异常时退回降级通道，绝不阻塞启动；**写失败（返回 `false` / `fail`）不得视为已持久化**。
  - **水合式同步读写**：容器 API 是异步的，启动时把全部 `aios_*` 键**并发**读进内存缓存（每个 key 各自 800ms 超时，整体最坏 ~800ms，不拖长开机；与 1.8s 开机动画并行），此后业务代码读写仍是同步的，不改调用点。
  - **迁移与一致性**：读时以容器值为准、写时**两条通道都写**（镜像同步先行、容器异步跟上，任一写成功即算成功），两侧缺哪边补哪边且**不删除任何一份**，客户端升级/降级都不丢数据；`localStorage` 读写一律吞异常（容器不保证其可用与持久）。
  - 设置页「关于本机」显示当前缓存通道与用量（容器通道取 `getStorageInfo` 的 `currentSize / limitSize`，单位 KB）；容器写入失败时该行如实改为「容器 Storage 写入失败 · 已回落 localStorage」。
  - **统一存储契约**：本层与 `vibetool/offwork-heatmap`、`vibetool/mood-diary` 同构（双通道 / 回调 + 超时 / 镜像先行 / 容器优先回填 / 后台对齐 / 通道可见），三处改动请同步。
  - 容量约束：单 key ≤ 1 MB，全部缓存 ≤ 10 MB。本项目存档均为小体量文本：`aios_stats` / `aios_mode` / `aios_wall` / `aios_wmask` / `aios_sound` 为定长小值，`aios_phone_records` 硬上限 5 条，`aios_sms_messages` 只随人工输入增长——超出上限时容器写入返回失败，已被容忍（降级通道仍有值），不会崩页面。
- **性能**：zip ≤ 2MiB 建议线（当前 ~1.5 MB，含 Three.js / 月面贴图 / 36 张相机素材）；壁纸用 CSS 渐变而非位图；**WebGL 仅限「月球天气」一款应用**（Three.js 本地随包，§4.9），其余应用一律纯 DOM/CSS，外壳与主屏不得依赖 WebGL。**音效不出音频文件**：整层为 Web Audio 实时合成（§4.11），不占包体、不引外部资源。
- **自检**：`_dev/smoke_test.py` 扫描产物不含超基线语法 token、不含禁用 API 字符串； playwright 无头跑导航栈与安全区变量断言。

---

## 6. 工程与打包

- `_dev/build.py` 单源 → 根 `index.html`（内联 CSS）+ 根 `main.js`（zip 内相对引用）；相机取景清单由它扫描 `assets/cam/` 注入，**应用商店清单由它解析仓库根 `TRACKS.md` 注入**（两者都是「构建期派生、不手抄」，见 §4.12）。
- `_dev/make_cam_photos.py` **借材派生**：只读 `vibeknow/world-food-3d/assets/foods/` 的美食图（36 张，跳过 `_placeholder`），统一收口成 `assets/cam/food-*.webp`（512×512 / q80，与来源同尺寸不缩图）**入库**，随 zip 分发；产物体积受小工具门禁约束（`performance-budget.md`：10 MiB 上限、2 MiB 建议），**加来源/换规格前先算体积账**、不得在此脚本外手工改名增删（清单由目录扫描得出）。
- `_dev/build_zip.py` 打小红书规范 zip：**先跑前置门禁**（脚本外置 / 无内联事件 / 无 `eval` / 无外部资源 / 安全区写法 / 禁用能力零命中 / **存储通道与版本判断** / **Three.js 与贴图本地化** / **相机素材随包** / **应用商店清单与 `TRACKS.md` 一致**），不合规直接拒绝打包；再构建 `dist/`（`index.html` + `main.js` + `assets/`）并把 **`dist` 的内容**（而非目录本身）压成 `ai-os.zip`，保证 `index.html` 位于 zip 根。**zip 布局**：根层 `index.html` / `main.js` + `assets/` 子树（`three.min.js` / `moon.jpg` / `avatar.png` / `cam/*.webp`）；打包脚本显式断言「顶层只能是 `assets/`、至多再下一层（`assets/cam/`）、无绝对路径与 `..` 逃逸」（规范只要求 `index.html` 在根，其余可自由分目录，见 `zip-artifact-spec.md` §1）。
- **受扫描的只有我们自己的代码**（`index.html` + `main.js`）：`three.min.js` 是上游产物，其中的 `http(s)` 字样与其内部 loader 不在运行时被触发（本项目只用静态场景 + 本地贴图），故不纳入禁用能力/外部资源正则；门禁改为单独校验「three.min.js 为本地相对引用、页面无 `https?://` 的 script src」。
- 交付前跑 `.skill/minitool-zip-builder/scripts/audit_artifact.py`（或 .mjs）审计，错误清零；zip 与 `dist/` 均被根 `.gitignore` 忽略（`*.zip` / `dist/`），不入库。
- 截图回归：playwright 390×844 移动视口逐页截图入 `_dev/_shots/`（临时，不入库）。

---

## 7. 分期计划

- **Phase 1（外壳与导航）**：✅ 已完成。单源工程骨架、安全区/净空、状态栏、主屏+Dock+小组件、底部三大金刚键、应用栈、多任务轮播、设置（壁纸/深色/关于本机）、主题与质感 token。
- **Phase 2a（代表游戏）**：✅ 已完成。计算器（对错判定）、闹钟（守时）、日历（扫雷）按 §4.7 落地，含 `aios_stats` 统计存储。
- **Phase 2b（其余应用）**：✅ 已完成。助手、日程、电话、短信、相机、统计按 §4.8 以 v1 逆向还原，v1 数据与恶搞文案已进单源。
- **Phase 3（打包与审计）**：✅ 已完成。`_dev/build_zip.py`（前置门禁 + dist + `ai-os.zip`，48 KB）、`audit_artifact.py` 审计 PASS（0 warning）、兼容扫描与截图回归纳入 `_dev/smoke_test.py`（含容器存储通道用例），README / DESIGN 同步；`_dev/smoke_test.py` 一并修掉三处历史漂移（`.rc-clear` 的 flex `gap` 基线违规、日程用例的选择器停在上版 UI、reload 断言抢在 1.8s 开机屏前执行）。
- **Phase 4（真机精细度）**：✅ 已完成（先于 Phase 3 落地）。应用页整屏底色与状态栏沉浸、内容撑满去中段空洞、主屏网格改 `start`、图标配色去重与中性投影、Dock 圆角 28px/等距/与主屏同尺寸、状态栏内容下移、钱包窄卡折行修复、多任务卡片缩略图、视图切换 `scale+opacity` 动效、相机侧键补样式、壁纸 swatch 预览对齐实际壁纸。
- **Phase 5（月球天气）**：✅ 已完成。把 `vibeknow/moon-3d` 整合为「月球天气」应用（§4.9），入口是主屏天气小组件。定稿形态按用户口径收敛为**只留天气**：整页单一读数视图（12 格），**月相由当前时间推算**（不设滑块），**取消自动自转**，只保留拖动旋转与缩放；**渲染直接采用 moon-3d 的 Three.js 场景**（本地 `assets/three.min.js`，与 `vibeknow/earth-3d` 同款引入方式），月面贴图沿用原图并随包分发。`_dev/smoke_test.py` 断言：小组件入口、单画布、WebGL 起得来、贴图 `data-tex=ok`、本地 three.min.js、仅天气（无分段/滑块/开关/科普块）、月相与照亮比例与测试独立复算对拍、静置 900ms 画面字节不变（证明不自转）、拖动改 `theta`、滚轮改 `radius`、**注入固定时钟**验满月与蛾眉月两种相位的读数与画面差异，返回后主屏仍 7+4。
- **Phase 6（相机借材 + 温控）**：✅ 已完成。相机取景框由「随机渐变 + 圆/★/♥」改为**轮播实际素材图**（§4.8）：新增 `_dev/make_cam_photos.py` 从 `vibeknow` 借材派生 `assets/cam/*.webp`（入库随包）；取景清单由 `build.py` **扫描目录注入**（不手抄文件名），每 3s 随机换一张且不与上一张重复、新图解码完再换 `src`。随后按用户口径两步收敛：① **去掉预览逗留**（无「重拍 / 保存」，拍完立刻换下一张、并重置 3s 计时）、加**镜头温控**（温度 = 拍照频率的函数：每次 +1.5℃ / 0.5℃·s⁻¹ 回落 / ≥43.5℃ 转红 / ≥45℃ 封锁快门）；② **只留美食一套**（36 张，512×512 / q80，约 0.95 MB），猫咪与恐龙素材及其来源一并删除。打包脚本放行 `assets/` 子树（`assets/cam/`）并加「相机素材随包 / 清单指向包内相对路径」两条门禁；`smoke_test.py` 断言「素材在包内（前缀 `food-`）、已解码上屏、铺满取景框、3s 换图不重复、无预览控件、拍完即换、连拍升温、过热转红与封锁、封禁时不再拍、静置散热后恢复」。zip 439 → 约 1.5 MB（audit `PASS: 41 file(s), 0 warning(s)`）。
- **Phase 7（系统音效）**：✅ 已完成。全系统统一音效层（§4.11）：**Web Audio 实时合成 25 个系统语义音效**（零音频文件），文档级 `click` 委托 + `data-sfx` 标注，覆盖导航、全部应用判定（计算器 / 助手 / 日程 / 日历 / 闹钟）、相机快门与过热、拨号通话、消息收发、多任务与开关；**首个手势前不建 AudioContext**（躲开自动播放策略 + 控制台告警）；设置页新增「系统音效」开关（`aios_sound`，双通道落盘，可整体静音）。`_dev/smoke_test.py` 断言：词表齐备、首个手势前不建上下文、点击后发声计数与上下文状态、判定与快门出声、静音后计数不涨且跨会话保持、无 Web Audio 时静音降级不报错、水合键数含 `aios_sound`；`_dev/build_zip.py` 加「Web Audio 合成 / 零音频文件 / 可静音」三条门禁。
- **Phase 8（应用商店）**：✅ 已完成。新增「应用商店」应用（§4.12，主屏第 7 格）：介绍**整个 ai-dev-kit 仓库已开发的小工具**，按 `TRACKS.md` 四分类分组，可按分类筛选。清单**构建期由仓库根 `TRACKS.md` 派生**（`build.py` 的 `store_apps()` → 注入 `main.js` 的 `__STORE_DATA__`，中文不转义），跳过 `.skill/`；每项折成商店口径：副标题由定位折（`store_tagline()`）、上架标签由物料状态折（`store_state()`）。**第一版把索引字段直接搬上屏（目录路径、状态原文、索引长句），整页读起来像 README，已按用户口径返工**：目录与状态原文一律不上屏，卡片一行一卡、静态不展开，`TRACKS.md` 文本经 `esc()` 转义。`_dev/smoke_test.py` 主屏图标数 6 → 7，并新增商店用例：与自带的独立解析 + 两套折算规则复算**三方对拍**（分类名与顺序、逐项 分类 + 名称 + 目录 + 定位 + 状态全量续存、副标题与上架标签折算）、卡片数 = 项目数、分组数 = 分类数、店头总数、分组标题、**目录路径与状态原文零上屏**、招牌图标相邻不同色、本机标「已安装」、分类筛选只剩该分类且计数正确、切回全部恢复、无 console 错误；`_dev/build_zip.py` 加「清单与 `TRACKS.md` 一致（构建期派生，非手抄）」与「含四大分类」两条门禁。

> 分期范围以用户确认为准；若要求一次到位，则 Phase 1+2 合并执行。

---

## 8. 验收清单

- [x] 顶部预留带 = `--top-gap` + safe-top（无宿主 50px / in-app 70px，均有普通值兜底不得塌陷）；无顶部返回钮；**in-app 隐藏自绘状态栏（不叠出两层状态栏），无宿主时照常显示**。
- [x] **in-app 与纯浏览器直开布局逐项一致**：半行组件并排、Dock 同尺寸不挤压、应用页左右不额外内缩；Dock 与金刚键栏之间留白 ≥24px。
- [x] 底部三大金刚键可用：返回逐层出栈、主页回主屏、多任务轮播恢复/关闭；多任务遮罩不挡金刚键、**空白处与空态点按均可关闭**；**注入底部安全区时栏高 = 52 + 安全区，键不被挤压到栏顶**。
- [x] 默认浅色壁纸、无文字辉光、状态栏时间居左/图标居右、Dock 无标签。
- [x] 天气小组件点按进入「月球天气」，且**整页只有天气**（无分段 / 无月相滑块 / 无显示设置 / 无科普块）；**月相按当前时间推算**、**不自转**（静置 900ms 姿态与画面字节均不变，拖动才动）；观测台用**本地 Three.js** 按 moon-3d 同款场景渲染（WebGL + sRGB + ACES + 星空天球 + 双层辉光 + 原版月面贴图），太阳方位角随真实月相走；无 WebGL / 上下文丢失时降级提示且读数照常；返回后主屏仍是 7 图标 + 4 坞位。
- [x] **进入任意应用后壁纸不再透出**（状态栏区/内容区/底部导航铺 `--app-bg`），回主屏恢复壁纸。
- [x] 主屏/计算器/助手等页无半屏空洞；操作区贴底；空态有引导；窄卡数字（钱包余额）不折行。
- [x] 钱包余额**默认打码**（`￥ -****`），点按才显示 `￥ -99,999`，再点回打码，状态可持久化。
- [x] 语义色统一；指标条主次分明。
- [x] 产物 ES2017/Chrome61：无超基线 JS 语法、CSS 基线+增强、禁用 API 零命中。
- [x] **存储合规**：容器 Storage JS API 优先、`localStorage` 仅降级；版本门槛（忽略末 3 位 / 逐级判空 / 异步兜底 / 取不到即不支持）；写失败可容忍；降级通道与容器通道双向迁移一致；key 前缀 `aios_`。
- [x] **统一音效**（§4.11）：全系统一张音效表 + 一条总线 + 一个开关，Web Audio 实时合成、零音频文件；可交互元素默认出声、`data-sfx` 指定语义、判定 / 快门 / 挂断等由动作发声不叠音；首个手势前不建 AudioContext；设置页可整体静音并持久化 `aios_sound`；无 Web Audio 时静音降级、交互照常（不抛错）。
- [x] **相机取景为包内实际素材**（美食 36 张 / 每 3s 随机换且不重复 / cover 铺满），**拍完即换下一张**（无预览页、无重拍/保存）、**温度随拍照频率升降并会封锁快门**（连拍升温→转红→封禁→静置散热后恢复），不联网、不外链、不申请摄像头权限。
- [x] **应用商店**（§4.12）：介绍整个 ai-dev-kit 已开发的小工具，按 `TRACKS.md` 四分类分组，可分类筛选；清单**构建期由仓库根 `TRACKS.md` 派生**（非手抄，缺文件即拒绝构建），跳过 `.skill/`；卡片是商店形态（招牌图标 + 名称 + 折算副标题 + 上架标签），**目录路径与物料状态原文零上屏**、一行一卡不展开；与自检脚本自带的独立解析与折算复算**三方对拍**（分类名与顺序 / 分类 + 名称 + 目录 + 定位 + 状态全量续存 / 副标题与标签折算）；招牌图标相邻不同色，本机那行标「已安装」；纯展示不跳转、不联网。
- [x] zip 审计通过（`PASS: 41 file(s), 0 warning(s)`，~1.5 MB，含本地 Three.js、月面贴图与 36 张美食素材）、`index.html` 位于 zip 根且子目录只在 `assets/` 子树内；playwright 自检 ALL PASS。
- [x] v1 完整保留于 `archive/v1/`，git 可追溯。

> 说明：以上为**静态 + 无头浏览器（Chromium 390×844）自动化**结论；**真机（iOS / Android WebView 61 基线机）未实测**，容器端 Storage 的真实读写与用量以真机日志为准。
