# 核战危机 DEFCON — 设计文档

> 本文档固化项目立项阶段（2026-09-08）讨论确定的全部设定，是后续开发的唯一参照。
> 最后更新：2026-09-09 16:00（§11.6 全部落地；§12 P0-2 + P1 全部完成；新增 §12.3 本轮变更清单与待定 2 项）
>
> **文档—代码一致性**：2026-09-09 全量核对过一轮。后续改 `src/` 里的数量型设定（城市数、单位数、事件卡数、perk 参数），
> 必须同步本文件对应章节，并同步 `tests/headless.js` 里的硬编码断言（当前：62 城 / 28 卡）。

---

## 1. 项目概览

- **定位**：小红书 VibeCoding 大赛 `#vibegame` 互动游戏赛道作品，以「小工具」形式上传挂载
- **形态**：3D 球面战略游戏，**竖屏移动端优先**，离线运行、不联网、无构建步骤
- **核心体验**：危机博弈 → 全球热核战争 → 同归于尽计分
- **灵感来源**：Introversion《DEFCON》(2006)
- **复用来源**：`vibeknow/earth-3d` 的 three.js 运行时、地球贴图与经纬度数学
- **投稿截止**：2026.9.21（官方已延期，原定 9.7；评审与公布日期待官方更新）
- **一局时长**：约 6–8 分钟

### 1.1 与《DEFCON》的关系

保留内核，砍掉 RTS 操作外壳。DEFCON 让人上瘾的是「什么时候打、打谁、留几枚反击」，不是「拖着航母画线、给战斗机下指令」。后者占实现量六成却不是内核，全部砍掉。

| 保留 · 决策内核 | 砍掉 · 操作外壳 |
|---|---|
| 阶段倒计时升级，时间逼你动手 | 战斗机与轰炸机空战 |
| 核弹有限且不可逆，打一枚少一枚 | 航母与战列舰海战 |
| 雷达视野与单位隐蔽 | 陆军入侵与领土占领 |
| 伤亡差计分，死得少的才算赢 | 拖拽部署与槽位管理 |
| — | 外交结盟与背叛 |

---

## 2. 世界设定

### 2.1 阵营（六个，全部虚构）

**半架空**：不出现任何真实国家名，也不出现真实政治军事组织名（北约 / 华约 / 欧盟等），
但阵营名带明确地缘指代，玩家一眼能认出「这是美洲、那是东亚」。代号沿用 NATO 音标字母。

| 代号 | 名称 | 主色 | 地理区块 | 核姿态性格（trait） | 鹰鸽系数 |
|---|---|---|---|---|---|
| ALFA | 北方联邦 | 蓝 `#378ADD` | 北亚 · 北欧 · 北极圈 | 倚核自重·先手威慑 | +0.90 |
| BRAVO | 美洲同盟 | 绿 `#639922` | 北美 · 中美 | 全域打击·精算得失 | +0.70 |
| CHARLIE | 欧亚公约 | 青 `#1D9E75` | 欧洲 · 地中海 · 中东 | 外交优先·稳健克制 | 0.00 |
| DELTA | 东亚联盟 | 红 `#E24B4A` | 东亚 · 西太平洋 | 不首先用·极度审慎 | −0.80 |
| ECHO | 赤道共同体 | 橙 `#EF9F27` | 拉美 · 非洲 · 东南亚 | 无核地带·左右逢源 | −0.20 |
| FOXTROT | 南洋联邦 | 紫 `#7F77DD` | 大洋洲 · 太平洋群岛 | 无核·避战自保 | −0.40 |

> **性格口径修正（2026-09-08）**：初版把东亚写成「强硬·不惜一战」（+0.90）、美洲写成「主和·厌战」（−0.60），
> 与现实核门槛**完全相反** —— 现实中核威慑依赖最深、核门槛最低、最常以核讹诈补常规力量不足的是北方（ALFA），
> 而公开承诺不首先使用、经济依存最深、最怕局势失控的恰恰是东亚（DELTA）。
> 沿用刻板印象会让懂地缘的玩家一秒出戏，故按**现实核姿态**重排六方系数。
> ⚠ 红线不变：不出现真实国家名与真实政治军事组织名，性格只作系数上的抽象，不指名影射具体国家。

> **命名口径变更（2026-09-08）**：原名「北境联邦 / 西海同盟 / 中央联合体 / 东陆公约」是纯方位词，
> 配上真实坐标的城市后现实感撕裂 —— 玩家看到燕京、长港、雾京，却不知道自己在替谁打仗。
> 改用带大区指代的集团名后，地缘关系一眼可读。
> ⚠ 红线不变：**不得使用真实国家名或真实政治军事组织名**。核打击真实国家有过审风险，
> 也会把作品拖进现实政治叙事，与本作「相互确保毁灭」的立意不符。

阵营边界**不按真实国界**，按大区粗略划分，避免与现实政治实体一一对应。

### 2.2 城市（62 座：ALFA–ECHO 各 10 座，FOXTROT 12 座）

城市位置与人口为**公开信息**（常识，不需要侦察）。

> **口径变更（2026-09-09）**：FOXTROT 因 `perk: spread`（人口稀疏·城市多）追加 2 城至 12 座，总数 60 → 62。
> ⚠ 凡是写「60」「每阵营 10 座」的地方都已作废，`tests/balance.js` 曾把失城数写死成 `10 - citiesAlive`，
> 导致 FOXTROT 的失城统计恒少 2 —— 按阵营实际城数计算，勿写常量。

> **口径变更（2026-09-08）**：城市坐标不再随机撒点，**经纬度取真实城市坐标**，名称改为该城的中文别称
> （取地貌／别号，不直接用真名），并在 `data.js` 里保留 `real` 字段记录原型供维护核对。
> 理由：随机撒点会让城市飘在海里、阵营区块边界失真，球面沙盘的地理说服力全丢；
> 用真实坐标后，跨洲飞行距离、雷达覆盖、弹道时长的观感才与玩家直觉一致。
> 代价是同城圈必然出现（西雅图↔温哥华 195 km），故最小间距约束从 400 km 放宽到 150 km。

| 阵营 | 别称（真实城市原型） |
|---|---|
| ALFA | 白京（莫斯科）· 北港（圣彼得堡）· 极港（摩尔曼斯克）· 使城（阿尔汉格尔斯克）· 岭城（叶卡捷琳堡）· 新原（新西伯利亚）· 湖京（赫尔辛基）· 桦岛（斯德哥尔摩）· 雪湾（安克雷奇）· 冻城（雅库茨克） |
| BRAVO | 雾湾（洛杉矶）· 金岬（旧金山）· 雨港（西雅图）· 枫湾（温哥华）· 风城（芝加哥）· 油港（休斯顿）· 长港（纽约）· 湖城（多伦多）· 谷京（墨西哥城）· 高城（丹佛） |
| CHARLIE | 雾京（伦敦）· 光城（巴黎）· 铁京（柏林）· 丘城（罗马）· 台京（马德里）· 平城（华沙）· 峡京（伊斯坦布尔）· 沙京（开罗）· 山京（德黑兰）· 石港（雅典） |
| DELTA | 燕京（北京）· 江口（上海）· 穗港（广州）· 锦城（成都）· 江城（武汉）· 秦城（西安）· 沈城（沈阳）· 东湾（东京）· 汉阳（首尔）· 原京（乌兰巴托） |
| ECHO | 糖港（里约热内卢）· 松京（圣保罗）· 旱城（利马）· 岭京（波哥大）· 泻港（拉各斯）· 河京（金沙萨）· 高原（内罗毕）· 椰港（雅加达）· 狮港（新加坡）· 棕城（马尼拉） |
| FOXTROT | 帆港（悉尼）· 金湾（墨尔本）· 晴港（布里斯班）· 孤港（珀斯）· 酒湾（阿德莱德）· 北门（达尔文）· 云港（奥克兰）· 岬城（惠灵顿）· 珠屿（檀香山）· 珊屿（苏瓦）· **塔港（霍巴特）** · **震城（基督城）** |

- 单城基准人口 4–14 百万，每阵营**基准**总量 85–94 百万
- 城市被核打击后人口按比例损失，光点熄灭，球面留下焦痕
- **§12 城市定位环**：每城在光点外侧再描一圈细白环（直径 ≈ 光点的 1.55 倍），
  让玩家一眼能数清某片区域有几座城、与邻城的相对位置。光点本身表达「这里有城 + 人口多不多」，
  环只承担「坐标锚」职责，不带阵营色（否则 DELTA 红环套红光点过曝成实心团）。
- `create()` 时按阵营 perk 乘人口倍率后取整（下限 3），**实际开局**总量 75–113 百万：

| 阵营 | 基准 | 倍率 | 开局实际 |
|---|---|---|---|
| ALFA | 92M | ×1.0 | 92M |
| BRAVO | 91M | ×1.0 | 91M |
| CHARLIE | 86M | ×1.0 | 86M |
| DELTA | 87M | ×1.3 | 113M |
| ECHO | 85M | ×1.0 | 85M |
| FOXTROT | 94M | ×0.8 | 75M |

- ⚠ 文档 §11.8 与 `tests/` 报告里若出现「人口总量 85–92M」是改 perk 之前的旧口径，以本表为准

### 2.3 地图表现

- 保留真实地球几何与 `earth.jpg`（2048×1024 等距圆柱贴图）
- 叠加**风格化滤镜**：降饱和、压暗、叠青灰、加矢量网格，做成军事沙盘质感
- 政治实体全虚构，做到「形状像地球，国家不存在」
- **深空环境（v0.3）**：900 颗星的天球背景 + Fresnel 大气辉光壳（1.06R、只渲背面、加法混合）——
  「一颗悬在深空里的星球」比纯黑背景更有临场感；gl_PointSize 必须乘 devicePixelRatio，
  否则高分屏上星星小于 1 物理像素、整片星空消失

### 2.4 开局阵营选择

载入完成后先弹**阵营选择弹窗**，六方各一行：色块 + 名称 + 地理区块 + 性格标签，选完才建局。

- 玩家阵营必须在 `create()` 时就定下来 —— 雷达覆盖圈、己方单位可见性、城市列表的敌我划分全依赖它，
  开局后再改要重建半个渲染层，不如一开始就问。
- 顺序是**先 WebGL 预检、再弹选择**：不支持 3D 的设备上，让玩家挑完才告诉他跑不了是纯粹的浪费。
- 「再来一局」走 `location.reload()`，因此每局都可以换个阵营。

### 2.5 竖屏单手操作（v0.3 移动端重构）

**定位口径（2026-09-08）**：本作就是移动端游戏，只维护一套 390×844 基准的竖屏布局，
不再为桌面做第二套 —— 桌面浏览器里以 `#app`（max-width 480px）居中成一条竖屏呈现。

| 区块 | 内容 | 高度预算（844px 基准） |
|---|---|---|
| 顶栏 `#top` | **两行**（2026-09-10 移动端布局修订）：第一行 DEFCON 等级 · 阶段（居中）· 当前阵营（色块+名称，贴右）三段分布；第二行危机值条独占整行 —— 390px 手机上单行内危机值条只剩 ~70px、挤成一团，独占一行后进度条有完整宽度可读 | ~85px |
| 主视觉 | 地球（竖屏距离 7.4，球径占屏宽约 93%；开场从 12 缓推进场） | 其余全部 |
| 底部情境面板 `#sheet` | 危机期=事件卡，战争期=发射条，二者互斥；简报期整块隐藏。指令条只留一行统计：规模 / 弹头（战争期换战损）+ 最右端倒计时；回合数只体现在事件卡标题的回合徽章上（2026-09-10 修订：底部「第 X 回合」文本与卡片标题重复故删，倒计时移到统计行最右）。音效·加速·抽屉三个按钮**悬浮在面板上缘之外**（`#hudBtns` 绝对定位 `bottom:100%` 贴面板顶边，不占面板内部空间，随卡片高度自适应升降） | ~165px |
| 抽屉 `#drawer` | 阵营存活人口 / 单位图例 / 城市列表，右侧滑出 | 满高，84% 宽 |

交互约定：

- **两段式发射**：核弹不可逆，没有一键发射。第一段选目标（轻点地球光点或抽屉城市行），
  第二段按「发 射」确认；**已选中目标再点一次同一目标等价于确认**（两次有意点击 = 确认，防误触不等于拖沓）。
  齐射按钮在 ×1 / ×3 间切换。
- **抽屉必须随选随收**：抽屉 z-index(30) 高于底部面板(12)，满屏高会盖住发射条 ——
  选中目标后自动收起；点地球也会收起。
- **拾取容差按屏幕算**：`pickCity` 用屏幕空间投影比距离（容差 = 画布宽 7%，22–44px），
  并做背面剔除；不用角度容差 —— 390px 宽的手机上 1° 只有约 1.6px，角度常量在缩放两端必然失真。
- **演出层**：核爆全屏白闪 `#flash`、DEFCON 1 边缘红脉冲 `#alarm.on`、
  危机值 ≥85 心跳预警 `#alarm.warn`、DEFCON 等级跃迁 `#defcon.bump`。
- 安全区适配 `env(safe-area-inset-*)`；触控目标最小 46px；`touch-action:none` + 双指缩放。

---

## 3. 单位系统

### 3.1 开局布阵梯度（§11.1 已落地：核武存量按阵营拉开）

三个兵种，**开局固定布阵**，玩家不拖拽放置。发射井 / 防空 / 雷达三者的数量**不再是六方统一**，
由 `DC.perkOf(code)` 统一给出（`src/data.js` 的 `perkSilosByFaction` / `perkMissilesPerSiloByFaction` / perk 分支）：

| 阵营 | 发射井 | 每井弹数 | **核弹总量** | SAM | 雷达 | 单位合计 |
|---|---|---|---|---|---|---|
| ALFA | 8 | 3 | **24** | 4 | 3 | 15 |
| BRAVO | 6 | 3 | **18** | 6 | 3 | 15 |
| CHARLIE | 6 | 3 | **18** | 4 | 5 | 15 |
| DELTA | 5 | 3 | **15** | 4 | 3 | 12 |
| ECHO | 6 | 2 | **12** | 4 | 3 | 13 |
| FOXTROT | 7 | 2 | **14** | 4 | 3 | 14 |
| **合计** | 38 | — | **101** | 26 | 20 | **84** |

> **口径变更（2026-09-09，§11.1）**：原「六方统一 6 井 × 3 弹 = 18 枚、合计 78 单位」已作废。
> 梯度逻辑：鹰派多弹（ALFA 24、BRAVO 18）、鸽派少弹（DELTA 15、ECHO 12）、南洋多井少弹（FOXTROT 7×2=14）。
> 与 §2.1 的 perk 体系对齐 —— 选阵营时「核弹基数」成为一条独立且可读的策略维度（选择界面直接展示 `factionMissiles()`）。

### 3.1b 战斗参数（可调）

| 参数 | 值 |
|---|---|
| SAM 拦截概率 | 0.55（BRAVO perk +0.08 → 0.63） |
| SAM 每波拦截容量 | 2 枚 |
| SAM 冷却 | 12 秒 / 枚（§3.1 旧表写「2 回合」，实现换算为 `samCooldownSec`） |
| 雷达覆盖角半径 | 45°（约 5000 km；CHARLIE perk +15° → 60°） |
| 核弹杀伤率 | 命中城市损失该城 85% 人口 |
| 导弹飞行时间 | 按大圆距离 15–40 秒 |
| SAM 覆盖角半径 | 9°（约 1000 km） |
| 城市防御层折算 | 命中杀伤 ×0.35，抵扣一层 |

> **SAM 覆盖半径 18° → 9°（2026-09-08）**：城市改用真实坐标后，18° 的覆盖圈能一次罩住整块大陆
> （欧洲、北美东部全境），实测总拦截率飙到 82%，108 枚弹只有 19 枚落地，核战阶段打不出战果。
> 下调到 9° 后拦截率回到 67%，与改坐标前的 68% 持平；附带把地缘造成的伤亡极差从 3.7x 压到 1.6x。

### 3.2 单位图标与图例（同一套图形）

三种单位在球面上用**与 HUD 图例完全相同的图形**表示（2026-09-08 起，此前是 3D 几何体）：

| 单位 | 图形 | 辨识特征 |
|---|---|---|
| 发射井 | 六边形 | 竖长六边，像井盖 |
| 防空 SAM | 三角形 | 尖顶朝上 |
| 雷达 | 椭圆 + 支杆 | 扁、带杆 |

实现口径：把图例里的三个图形画到 canvas 贴图，用**永远正对镜头的 billboard 平面片**渲染，
白色贴图 × 阵营色（InstancedMesh instanceColor）。不再用 3D 几何体 —— 立体剪影缩到十几个像素后
与图例线稿对不上，且位于球面边缘时会侧视退化成一条线；billboard 任何角度形状不变，「所见即图例」。

HUD 抽屉里的**图例**用与 3D 一一对应的 SVG 图形列出三者，外加城市光点一行，
并注明「敌方单位默认不可见 —— 只有被雷达捕获并弹道溯源标记后才会显形」。

---

## 4. 危机博弈系统（核心创新）

### 4.1 危机值 → DEFCON 等级

DEFCON 等级**不再由固定倒计时驱动**，而由六方共同选择推出。

| 危机值 crisis | 等级 | 解锁 |
|---|---|---|
| 0–19 | DEFCON 5 | — |
| 20–39 | DEFCON 4 | — |
| 40–59 | DEFCON 3 | — |
| 60–79 | DEFCON 2 | — |
| 80+ | DEFCON 1 | **核弹解锁** |

- 初始危机值 10
- **每回合基础漂移 +6.8**：世界在滑向战争，光靠选缓和选项拖不出和平，只能拖慢
  （原为 +4；性格改成现实向后六方整体偏鸽，实测 9/10 局爬不到 DEFCON 1，只能靠回合上限兜底，
  上调后 150 局实测：开战回合 min7 / 中位 11 / max14，撞上限 7/150）
- 事件选择修正 ±5–15，极端选项可达 ±30

### 4.2 事件卡

每回合（20 秒）弹出一张全球事件卡，**六方同时选择**。共 **28 张**（E01–E28），够一局不重复
（`maxRounds` 14 < 28，一局最多抽 14 张）。

> **口径变更（2026-09-09，§11.2）**：原 23 张 → 28 张。新增 E23–E28 六张，
> 用来承载危机博弈期的「实质操作」：补建核弹（`add_missiles`）、按人口基数增长（`boost_pop` mode:ratio）、
> 削弱/摧毁敌方设施（`degrade_facility` / `destroy_facility`）、获取敌方情报（`intel_city`）。
> ⚠ `tests/audit-d1.js` 第 7 节与任何写「22 / 23 / 25 张」的说明都已过时。

结构：标题 + 冷战风味描述 + 2–3 选项，每选项含危机值修正与可选副作用。

选项的 `crisis` 除了 ±30 的数值，还可以是字符串 `'MAX'` —— 选中即直接拉满（见 §4.4）。
当前挂在 E02「雷达误报」与 E23「敌战略轰炸机群转向我方」两张卡上，语义都是「误判/先发制人」。

| 事件 | 选项 A | 选项 B | 选项 C |
|---|---|---|---|
| 侦察机在敌境失踪 | 否认任务 −4 | 公开抗议 +9 | 派机搜救 +14 |
| 雷达误报 200 枚来袭导弹 | 立即反击 **+30** | 等待确认 −6 | 提升戒备 +8 |
| 敌方提议裁军谈判 | 接受 −14 | 拖延 0 | 拒绝并增兵 +8 |
| 我方雷达遭网络攻击瘫痪 | 报复 +11 | 静默修复 −3（雷达停摆 2 回合） | — |
| 公海舰船对峙 | 后撤 −7 | 保持对峙 +5 | 开火警告 +16 |

**副作用类型**：暴露己方某座发射井 · 揭示敌方雷达位置 · 某城防空 +1 级 · 直接人口损失 · 雷达停摆 N 回合。

所以不是单纯选「加还是减」，而是权衡。

### 4.3 AI 性格

五个 AI 共用**一个**决策函数，靠性格系数区分：

```
得分 = 危机值修正 × hawkishness + 副作用权重 + 随机扰动
```

`hawkishness ∈ [-1, 1]`，鹰派偏好激化、鸽派偏好缓和。一行代码让五个 AI 各有脾气——玩家会慢慢摸清「北方是火药桶，东亚在踩刹车」。

系数按现实核姿态取值（见 §2.1 表格），鹰派端是 ALFA（+0.90）与 BRAVO（+0.70），鸽派端是 DELTA（−0.80）与 FOXTROT（−0.40）。
这不是随机分配：谁先按按钮、谁一直在踩刹车，玩家能在两三局内摸出来，性格才立得住。

### 4.4 拉满机制（危机值直接顶到 100）

三个触发源，任一命中即把全局危机值置 100、DEFCON 降到 1、立刻进入热核战争，跳过「六方取均值」的常规结算：

| 触发源 | 位置 | 说明 |
|---|---|---|
| 选中 `crisis:'MAX'` 的选项 | 事件卡 | 语义是「误判/先发制人」，玩家与 AI 都能选（AI 有条件，见下） |
| 常驻「直接拉满危机值」按钮 | 事件卡底部 | 玩家**随时**可以主动引爆，不必等抽到带 MAX 的卡 |
| 任一枚核弹落地 | `resolveImpact` | 第一枚弹砸下去就没有回头路了 |

**AI 必须加闸门**：`crisis < CONFIG.aiMaxMinCrisis(70)` 时，MAX 选项对 AI 判为 `-Infinity` 直接排除。
没有这道闸门，六个 AI 里只要有一个鹰派（DELTA 0.9），第一回合就会把世界点了，危机博弈阶段彻底失去意义。
选 70 而非更低，是为了把开战时机钉死在 §6.1 设计的 8–14 回合内：从 10 出发、每回合净约 +7.8，爬到 70 恰是第 8 回合。

玩家不受此限 —— **主动引爆是玩家的特权**。这是给「不想再被 8–14 回合博弈拖着」的玩家留的快进键。

---

## 5. 弹道溯源系统（核心创新）

### 5.1 机制

平时敌方发射井**完全不可见**。敌方开火后，导弹飞行途中若进入我方雷达覆盖（角距离 < 覆盖半径）即被捕获，沿大圆弹道反向回溯已飞距离，得出估算发射点。

**拦截演出（v0.3）**：拦截成功时 sim 把 SAM 阵地坐标记在 `m.interceptor` 上随 fx 事件带出，
渲染层据此演完整过程——防空阵地点火 → 拦截弹沿离地弧线爬升 0.34s → 撞上来袭弹 → 爆闪 0.5s。
没有这段，玩家只看到空中凭空冒出一团光，不知道是谁打的、从哪打的。

**误差模型**：

```
σ = σ₀ + k × (雷达到估算点的角距离)
```

越远打得越准不了，误差越大。

- 估算区内若存在真实发射井 → 该井被**永久标记**，可发起反击
- 落在区外 → 只显示「怀疑区域」虚线圈，若干回合后消散

实现约 60 行。

### 5.2 博弈价值

这个机制给**先手打击加了代价**，是 MAD 内核在操作层的落地：

| 你的选择 | 后果 |
|---|---|
| 从近处发射 | 定位精确，你的井立刻被反击 |
| 从远处发射 | 误差大，敌人只知大概区域 |
| 分散多井齐射 | 一轮暴露多座井 |
| 集中单井发射 | 只暴露一座，但射速慢 |

「第一时间倾泻全部核弹」不再是无脑最优解：打得越猛，暴露越多，第二轮能打出去的就越少。

---

## 6. 核战与计分

### 6.1 流程

```
阵营选择（无时限）
    ↓
态势简报 10s
    ↓
危机博弈：每回合 20s × 8–14 回合（中位 11；约 1% 的局第 7 回合即被引爆；随时可按「拉满」直接开战）
    ↓
DEFCON 1 → 核战阶段 2–3 分钟
    ↓
核弹耗尽或计时结束 → 终局计分
```

### 6.2 计分

```
排名 = 各阵营剩余存活人口（百万人为单位）降序
```

赢不是「灭了对手」，是「活下来的人多」。终局按剩余存活人口排名；同分再比己方伤亡升序。
> **§11.8 修订（2026-09-09）**：原「得分 = 敌方伤亡 − 己方伤亡」等价但不完全相同
> （剩余 = 初始人口 − 己方伤亡，初始人口各阵营不同），玩家直觉更关心「谁活下来的人多」。
> score 字段保留供审计，排序键改为 popLeft。

---

## 7. 技术架构

### 7.1 目录约定

```
index.html              # 入口（内联样式 + HUD 骨架，脚本外置）
assets/
  earth-tex.js          # 224 KB，earth.jpg 的 base64 内联版（file:// 下外链 jpg 被 CORS 拒，见下）
  three.min.js          # r149，608 KB，本地无 CDN
  earth.jpg             # 2048×1024，512 KB（http 场景下的兜底，非必需）
tools/
  gen-earth-tex.js      # 由 assets/earth.jpg 生成 earth-tex.js（离线构建，不进提交包）
src/
  data.js               # 阵营、城市、事件卡、配置（纯 JS，不 fetch）
  geo.js                # 经纬度数学、大圆航线、弹道、溯源
  sim.js                # 阶段机、实体状态、tick、伤害结算
  ai.js                 # AI 决策
  render.js             # three.js 渲染（球体、单位、导弹、特效）
  ui.js                 # HUD、事件卡、城市列表
  game.js               # 主循环、事件绑定
tests/                  # 无头校验脚本，不进提交包
  headless.js           # 117 项功能断言（对不对）
  balance.js            # 大样本节奏与胜负分布（好不好玩）
  audit-d1.js           # 数据层可读性审计（合不合理）
  smoke-browser.js      # 浏览器端冒烟（GPU / 报错 / 帧率）
  smoke-mobile.py       # 移动端真机/模拟器冒烟
dist/                   # 提交用的 zip 包（当前为空，尚未打包）
docs/                   # 素材与截图
# 小工具打包规范见工作区根 .skill/minitool-zip-builder/（本目录不内嵌副本）
DESIGN.md               # 本文档
```

> **贴图加载链**（`render.js`）：内联 data URI（`earth-tex.js`）→ `assets/earth.jpg` → 纯色球体。
> 小工具常被以 `file://` 打开，此时外链 jpg 会被 CORS 拒绝、地球变纯色 —— 内联版是主路径，jpg 只是 http 兜底。
> 这条链 §7.1 早期版本未记录，导致「为什么有两个地球贴图」反复被问。

### 7.2 脚本约定

**无构建步骤**。7 个外置经典脚本，按依赖顺序写多个 `<script src>`，靠 `window.DC` 命名空间协作：

```
data.js → geo.js → sim.js → ai.js → render.js → ui.js → game.js
```

不使用 `type="module"`，不使用 `import` / `export`，不使用 top-level `await`。

**数据不写成 JSON 文件**：容器禁止网络请求，离线环境下 `fetch('./x.json')` 不可靠。全部数据以 JS 对象写在 `src/data.js`。

### 7.3 模拟循环

逻辑 10 Hz 固定步长 + 渲染 60 fps 插值。固定步长让 AI 行为与玩家操作完全确定，便于 `tests/` 无头断言。

### 7.4 复用 earth-3d 清单

| 资产 | 状态 | 用途 |
|---|---|---|
| `three.min.js` r149 | 直接复用 | 渲染底座 |
| `earth.jpg` 2048×1024 | 直接复用 | 等距圆柱底图 |
| `ll2v(lat,lon,r)` | 直接复用 | 经纬度转球面，UV 已对齐 |
| `radialTex()` 程序化贴图 | 直接复用 | 核爆闪光、城市光点、冲击环 |
| `latRing` / `meridian` | 改造复用 | 雷达扫描线风格 |
| 球坐标相机控制 | 直接复用 | 手写，无 OrbitControls 依赖 |
| `clouds.png` | **不复用** | 军事终端风格下是累赘 |

earth-3d 只有一张贴图，没有矢量国界、城市数据、单位系统——这些是本项目的全部工作量。

### 7.5 WebGL 预算（移动端保守档）

| 指标 | 默认档 | 降级档 |
|---|---|---|
| DPR | `min(devicePixelRatio, 1.5)` | `1` |
| 纹理边长 | 2048 | 1024 |
| 估算纹理显存 | ≤ 64 MiB | ≤ 32 MiB |
| 每帧 draw call | ≤ 100 | ≤ 50 |
| 每帧三角形 | ≤ 100k | ≤ 50k |
| 帧率目标 | 稳定 30 FPS | 24–30 FPS |

- 实际占用：`earth.jpg` 2048×1024×4 ≈ 8 MiB（含 mipmap 约 11 MiB），远低于预算
- 城市 60 用 `Points`、单位 78 用 `InstancedMesh`、导弹尾迹用 `Line`，**禁止每帧新建 Mesh**
- 必须处理 `webglcontextlost` / `webglcontextrestored`，并提供非 WebGL 兜底界面

### 7.8 手写 bloom 后处理（v0.3）

包内只有 `three.min.js` 核心，没有 examples 的 EffectComposer —— bloom 自己写，约 120 行：

```
场景 → rtScene(全分辨率)
     → 亮度提取 smoothstep(0.72, 1.0) → rtA(1/4 分辨率)
     → 可分离高斯 横→纵 ×2 轮（5 tap 线性采样版）
     → 与原画面加法叠加回屏（uStrength 1.25）
```

- 阈值取 0.72 起：染色的地球陆地亮度能摸到 0.5–0.6，阈值低了整个星球泛光糊掉；
  城市光核 / 导弹头 / 核爆都在 0.9 以上
- **降级**：建 RT 或编 shader 抛错 → 永久回退直渲，不重试；quality=0 不启用；
  `webglcontextrestored` 后重建离屏 RT；`resize` 时 RT 必须随画布缓冲重建，否则 bloom 分辨率错位
- 震动等表现层随机一律 `Math.random`，绝不碰 `state.rng`（决定性模拟红线）

### 7.9 核爆演出（四层结构）

| 层 | 时长 | 表现 |
|---|---|---|
| 瞬态白闪 | 1.15s | 峰值 0.5、暖白 `#fff8ea`（2026-09-10 移动端防频闪修订：原纯白 0.9 / 0.55s 急灭在手机上过于刺眼），10% 快速起亮 + 约 1s 缓慢退光；`ui.js pumpImpacts` 按 1.1s 最小间隔节流，齐射连续命中不重播（音效 / 震屏 / 焦痕不受节流影响） |
| 火球 | 2.6s | 先快涨后慢涨，颜色 白→橙→暗红，负责余烬感 |
| 蘑菇云尘柱 | 2.25s | 比火球延后 0.35s 升起，沿球面法线抬升、边升边暗边散 |
| 冲击波环 | 1.4s | 贴地表扩散 15° 弧长，负责「能量掠过球面」 |

- 落地同时：焦痕 Sprite 持久留球面、全屏白闪、相机震动（多弹叠加、上限 1.6）、
  **bloom 强度脉冲**（1.25 → 2.6，0.6s 衰减回来 —— 爆炸瞬间的过曝感来自后处理而不是把 sprite 无限放大）
- 导弹再入段增亮增粗（progress 驱动弹头 scale 与尾迹 opacity，尾迹由冷白转炽橙）
- 白闪触发读 `state.impacts`（sim 的单调计数）—— 不能读 `state.fx`，render 每帧 shift 清空队列，UI 永远读不到

### 7.6 小工具合规约束

- 脚本全部外置，无内联 `<script>`、无 `onclick=` 行内事件
- 经典脚本，无 `type="module"`、无 `import` / `export`
- 全部相对路径，无任何 `http(s)://` 外部引用
- 不使用 `fetch` / `XHR` / `Worker` / `eval` / `new Function` / WebAssembly
- JS 语法不超出 ES2017（Chrome 61 基线）
- 无 `<base href>`、无 `<iframe>` / `<object>`、无自建 CSP `<meta>`
- 打包时压缩 `dist/` 目录**内容**而非目录本身，确保 `index.html` 在 zip 根

> **已核查**：three.js r149 不含 `eval` / `new Function` / `Worker` / `WebAssembly`。其内部的 `fetch` 仅存在于 `FileLoader` 与 `ImageBitmapLoader`，本项目不调用这两个 loader，不触发。

### 7.7 包体预估

| 项 | 体积 |
|---|---|
| `three.min.js` | 608 KB（zip 后约 160 KB） |
| `earth-tex.js`（内联 data URI） | 224 KB（zip 后约 165 KB，base64 膨胀） |
| `earth.jpg` | 512 KB（http 兜底，可考虑不进包） |
| 代码 + 数据 | 约 70 KB |
| **含 jpg** | 约 1.4 MB，zip 后约 850 KB |
| **不含 jpg（推荐）** | 约 0.9 MB，zip 后约 400 KB |

低于 2 MiB 建议值，远低于 10 MiB 上限。

> **打包取舍（2026-09-09）**：`earth.jpg` 在 `file://` 下必定加载失败（CORS），带上它只是为 http 场景省 60 KB。
> 建议**只打包 `earth-tex.js`**，把 zip 从 ~850 KB 压到 ~400 KB；若实测 http 下内联版首屏解码有卡顿再考虑保留。

---

## 8. 分期计划

| 阶段 | 内容 | 状态（2026-09-09） |
|---|---|---|
| **M1** | 球 + 阵营色块 + 城市光点 + 事件卡 UI + 危机值驱动 DEFCON + 发射 ICBM + 弹道飞行 + 核爆 + 城市毁灭 + 计分 | ✅ 完成 |
| M2 | 弹道溯源、SAM 拦截、AI 完整决策 | ✅ 完成 |
| M3 | 音效、演出打磨、小红书物料 | ⏳ 音效未做 · 演出剩 11.6 脉冲 · 物料未做 |

> §10 的 D1–D10 为立项时排期，已全部走完，**当前执行计划见 §12**。

---

## 9. 待确认项

1. **地图路线**：当前方案为「真实地球几何 + 虚构政治实体」（推荐，省事且视觉好）。备选为「完全程序化生成虚构星球」，更干净但需新增大陆生成代码且失去 `earth.jpg` 复用。

## 10. 开发排期（截至 2026.9.21）

| 阶段 | 内容 | 建议 |
|---|---|---|
| D1 | `data.js`（60 城 + 6 阵营 + 22 事件卡）· `geo.js` | 数据先行，`geo.js` 可无头断言 |
| D2 | `sim.js`（阶段机 / 实体 / tick / 结算）· `ai.js` | 逻辑层，不依赖渲染 |
| D3 | `render.js`（球 + 阵营色块 + 城市光点 + 导弹 + 核爆）· `index.html` | 首次可见 |
| D4 | `ui.js`（HUD / 事件卡 / 城市列表）· `game.js` | 首次可玩 |
| D5–D7 | 弹道溯源 · SAM 拦截 · 手感调参 | M2 |
| D8–D10 | 截图 · 封面 · 小红书笔记 · 打包投审 | 物料 |

---

## 11. 待修订 / 体验问题记录（2026-09-09）

> 一次完整试玩后整理的 **9 项**修订点。below 每条含「现象 / 现状定位 / 修订方向 / 关联文件」。
> 落地时请逐项对照，并同步更新 AGENTS.md 与各 `README` 的口径。
>
> **2026-09-09 全量核对结果**（代码以 `src/` 为准，非本文件的描述）：

| # | 主题 | 状态 | 落地位置 |
|---|---|---|---|
| 11.1 | 核弹数量按阵营拉开梯度 | ✅ 已落地 | `data.js` `perkSilosByFaction` / `perkMissilesPerSiloByFaction`（24/18/18/15/12/14 = 101 枚） |
| 11.2 | 危机期建弹 / 情报 / 削敌设施 | ✅ 已落地 | 事件卡扩到 28 张；`sim.applyEffects` 接 `add_missiles` / `boost_pop(ratio)` / `degrade_facility` / `destroy_facility` |
| 11.3 | 雷达圈用阵营色 | ✅ 已落地 | `render.buildRadarRings` 读 `FACTIONS_BY_CODE[playerFaction].color` |
| 11.4 | `flyTo` 走最短弧 | ✅ 已落地 | `render.flyTo` 把 dTheta 归一到 `[-π, π]` |
| 11.5 | 拦截视觉同步 | ✅ 已落地 | `CONFIG.interceptDelaySec=0.34` + `sim.tryIntercept` 延迟 `m.alive=false` |
| 11.6 | 城市受击演出 | ⚠️ **八成** | 比例变暗 + 缩点 + 焦痕 0.6s 渐入已做；**受击瞬间辉光脉冲未做** |
| 11.7 | 弹头 / 尾焰 / 拦截弹配色统一 | ✅ 已落地 | `render.js` 顶部 `PALETTE` 常量集中管理 |
| 11.8 | 终局按剩余人口排名 | ✅ 已落地 | `sim.ranking` 主键改 `popLeft`；`ui.showOver` 末列显示剩余人口 |
| 11.9 | 城市列表「图例 × 数量」 | ✅ 已落地 | `ui.js` 内联 `SVG_SILO/SAM/RADAR` 徽章 |

> ⚠️ **11.1 + 11.8 的联合副作用未在本文档记录，且尚未调平** —— 见 §12 P0。

### 11.1 [✅ 已落地] 核弹数量需要重新设计，不应「除北方联邦外都一样多」

- **现象**：除 ALFA 外，其余五方核弹基数完全相同，阵营差异只剩颜色与几个 perk（防空/雷达/人口/外交），核武体量没有策略区分度。
- **现状定位**：`data.js` 里 `silosPerFaction:6`、`missilesPerSilo:3` → 全阵营统一 18 枚；只有 ALFA 经 `perkMissileSilos:8` 拿到 24 枚（`data.js:404-407` 与 `:499-506`），`perkOf` 据此给弹数（`data.js:540-559`）。其余五方纯属「配色不同」。
- **修订方向**：把每阵营的发射井数 / 每井弹数按定位拉开梯度（例如鹰派多弹、鸽派少弹、南洋多井少弹），形成「核武存量」维度的阵营差异化，与 §2.1 的 perk 体系对齐；`factionMissiles` 展示值与 `data.js` 事件卡 `add_missiles` 奖励一并联动。
- **关联文件**：`src/data.js`（CONFIG + `perkOf` + `factionMissiles`）、`src/sim.js`（`deployUnits`、`totalMissiles`）、`tests/balance.js`。

### 11.2 [✅ 已落地] 危机博弈阶段应能继续建造核弹，且选项要能获取情报 / 降低或摧毁敌方设施

- **现象**：危机阶段目前只是「选危机值 +1/−1」，博弈回合缺少「边谈边准备战争」的实质操作，博弈阶段意义不足。
- **现状定位**：`sim.applyEffects`（`src/sim.js:263-367`）已支持 `add_radar`/`add_sam`/`add_missiles`/`intel_city`/`expose_silo`/`reveal_radar`/`city_defense` 等 effect，**但**：
  - `add_missiles` 在 25 张事件卡中**从未被任何选项引用**（危机阶段无法增建核弹）；
  - 没有任何「降低 / 摧毁敌方设施（发射井、雷达、SAM）」类的 effect（`expose_silo` 只是暴露、`intel_city` 只是暴露关联设施，都不削弱战力）。
- **修订方向**：
  - 新增若干危机阶段选项，可在博弈期**补建己方核弹**（接 `add_missiles`）；
  - 新增危机阶段选项，**按己方人口基数增长人口**（在已有 `boost_pop` 基础上改为按阵营初始人口总量等比放大，而非固定 +N；扶养人口是博弈期「巩固基本盘」的核心取舍，与 §6.2 改为「看剩余人口计分」呼应）；
  - 新增 `degrade_facility` / `destroy_facility` 类 effect，让选项能**直接削弱或摧毁敌方发射井 / 雷达 / 防空**（与 §5 弹道溯源「先手暴露→被反击」形成因果闭环）；
  - 强化情报类选项（已暴露设施的命中效率、溯源误差等），让「情报战」成为博弈期的核心取舍。
- **关联文件**：`src/data.js`（事件卡 `DC.EVENTS`、effect 类型与文案）、`src/sim.js`（`applyEffects`、`forceWar` 前的 effect 结算）、`src/render.js`（设施被毁/降级的视觉反馈）、`tests/`。
- **2026-09-09 核对**：四项修订方向**全部落地** —— `add_missiles` 已挂在 E07 / E26 共 4 个选项上；`boost_pop` 新增 `mode:'ratio'`（E03 / E10 / E26，全境人口 ×1.03~1.05）；`degrade_facility`（E12 雷达半径减半 / E27 井弹减半 / E28 防空弹药减半）与 `destroy_facility`（E17 摧毁雷达 / E27 摧毁发射井）已接进 `sim.applyEffects`；`intel_city` 扩到 E25 / E28。
- **仍未做**：「已暴露设施的命中效率 / 溯源误差」的**情报收益兑现** —— 目前 `exposed` 只影响可见性（能不能点它反击），不影响打击精度。§5.2 承诺的「情报战成为核心取舍」因此只兑现了一半，见 §12 P1。
- ⚠ 「现状定位」里的「25 张事件卡」是撰写当时的数量，现为 28 张，勿再引用。

### 11.3 [✅ 已落地] 雷达警戒圈应与阵营颜色一致

- **现象**：玩家雷达覆盖圈是固定洋红，与所选阵营的主色无关，视觉语言不统一。
- **现状定位**：`render.buildRadarRings`（`src/render.js:441-452`）把圈线写死 `color: 0xff4fa0`，未读 `state.playerFaction` 的阵营色。注释里「换洋红是因为和经纬网太近」——但正确做法是贴合阵营色（或阵营色 + 高对比描边），而非硬换一个不相关的颜色。
- **修订方向**：用 `DC.FACTIONS_BY_CODE[state.playerFaction].color` 着色覆盖圈；若与经纬网（青灰）对比不足，再在圈上加描边/提亮，而不是脱离阵营色体系。
- **关联文件**：`src/render.js`（`buildRadarRings`、`syncRadarRings`）。

### 11.4 [✅ 已落地] 相机「视觉转换」有 bug，有时会绕大圈而非就近转过去

- **现象**：点城市列表/抽屉城市行 `flyTo` 时，相机有时不是就近平滑转过去，而是绕一整圈（大角度回转）。
- **现状定位**：`render.flyTo`（`src/render.js:972-977`）直接 `cam.tTheta = Math.atan2(v.x, v.z)`。theta 是 0–2π 的角度，目标可能落在 0.1 而当前在 6.2，线性插值会**走 6.2 → 0.1 的长弧（约 6 rad）**而不是 +0.1 的短弧 —— 即「绕大圈」。
- **修订方向**：在 `flyTo` 里把目标 theta 相对当前 `cam.tTheta` 归一到 `[-π, π]`（取 `tTheta + wrapToPi(target - tTheta)`），保证永远走最短弧；phi 同理注意极点 clamp。
- **关联文件**：`src/render.js`（`flyTo`、相机 `t*` 字段插值）。

### 11.5 [✅ 已落地] 拦截效果有 bug：拦截弹还没接触核弹，核弹就先消失了

- **现象**：SAM 判定拦截成功时，来袭核弹瞬间消失，但拦截弹还在升空爬升，画面出现「核弹凭空没了、拦截弹才飞过来」的割裂感。
- **现状定位**：`sim.tryIntercept`（`src/sim.js:465-491`）一旦 `state.rng() < prob` 立刻 `m.alive = false`，逻辑上导弹当场被击毁；但渲染层拦截弹演出要 `INT_RISE=0.34s` 爬升后才在目标点爆闪（`src/render.js:572-637`，`spawnInterceptor` 在 `m.interceptor` 带出）。逻辑击毁与视觉接触不同步。
- **修订方向**：把「实际击毁」推迟到拦截弹视觉抵达的时点（约 `INT_RISE` 后），逻辑层可记录「已命中待销毁」标记并延迟 `m.alive=false`/`pushFx('intercept')` 到对应帧；或渲染层在 intercept 触发瞬间先隐藏核弹头、保留尾迹至接触点再清。核心是**逻辑判定与视觉接触对齐**。
- **关联文件**：`src/sim.js`（`tryIntercept`、`updateMissiles`）、`src/render.js`（`spawnInterceptor`、`pumpInterceptors`、`pushFx` 的 `intercept` 分支）。

### 11.6 [✅ 已落地] 城市被打击的视觉效果仍显怪异，需优化

- **现象**：城市光点被毁后的表现不自然（骤暗/泛光残留/焦痕突兀等），整体观感割裂。
- **现状定位**：城市用加性混合 `Points`（`src/render.js:304-354`），被毁只把 `aAlpha` 从 1 降到 `0.12`（`syncCities:346-354`），叠加 bloom 后会残留「幽灵光点」；焦痕是独立 `Sprite`（`addScorch:356-366`）直接贴在球面，与光点熄灭缺乏过渡。
- **修订方向**：重做城市「受击 → 熄灭 → 留焦痕」的连贯演出：受击瞬间辉光脉冲（而非恒亮）、熄灭后光点彻底淡出（alpha→0 并缩点）、焦痕随之柔和浮现；可考虑人口损失按比例做「部分变暗 + 缩点」而非二值化。注意保持 bloom 阈值（0.72）下不残留伪光。
- **关联文件**：`src/render.js`（`buildCities`/`syncCities`/`addScorch`、CITY_FS 着色）、`src/sim.js`（`resolveImpact` 的 pop 损失与 `defCharges`）。
- **2026-09-09 核对**：已做 —— ①被毁 alpha→0 且缩点 0.5×（幽灵光点消除）；②受损按 `pop/pop0` 比例变暗（alpha 0.35–1.0）并缩点；③焦痕初始 opacity 0、0.6s ease-out 渐入到 0.85（`pumpScorch`）。
- **剩余未做**：**受击瞬间辉光脉冲**。当前受击只是下一帧变暗，缺一记「被打中」的闪。做法建议：`resolveImpact` 时往 `state.fx` 的 impact 事件带出 `popLossRatio`，render 在对应城市光点上叠一个 0.25s 的 `aSize` 脉冲（×1.8 回落），配合 bloom 阈值 0.72 只在峰值短暂过阈。另需真机确认：脉冲不能让 bloom 在密集轰炸时糊成一片。

### 11.7 [✅ 已落地] 核弹弹头与尾焰的颜色希望整体性更好

- **现象**：弹头（白）与尾焰（暖橙渐变）的色彩关系略显随机，整体调性不统一。
- **现状定位**：弹头 `slot.head.material.color.setHex(0xffffff)`（`src/render.js:503`）；尾迹基色 `0xffd8a0`、再按 `setRGB(1, 0.85 - p*0.30, 0.63 - p*0.45)` 由冷白转炽橙（`src/render.js:506-508`）；拦截弹线又是 `0x9fe0ff` 蓝（`src/render.js:587`）。三者各自为政。
- **修订方向**：统一一套「来袭导弹」色板：弹头与尾焰同色系（如从炽白核心 → 暖橙 → 暗红的余烬感），与核爆火球色（`src/render.js:747`）与阵营色呼应；拦截弹（己方反击）用清晰的对比色（阵营青/蓝）以区分「我方拦截」与「敌方来袭」。建议抽成 `TEX` 之外的配色常量集中管理。
- **关联文件**：`src/render.js`（`syncMissiles`、`buildMissilePool`、`buildInterceptorPool`、`pumpInterceptors`、`buildFxPool`）。

### 11.8 [✅ 已落地] 最终评分应看「剩余人口数」

- **现象**：当前终局排名按「敌方伤亡 − 己方伤亡」排序，但玩家直觉更关心「谁活下来的人多」。
- **现状定位**：`sim.ranking`（`src/sim.js:672-686`）的 `score = killed - casualties`，排序键为 `(b.score - a.score) || (a.casualties - b.casualties)`；§6.2 的立意是「死得比他少」。这与「剩余人口」等价但不完全相同（剩余 = 初始人口 − 己方伤亡，初始人口各阵营不同）。
- **修订方向**：评分改为**剩余存活人口**（各阵营 `cities` 求和 `pop`，或 `pop0 总量 − casualties`），排名按剩余人口降序；保留「同分再比伤亡」等次级键。同步更新 §6.2 文案与 UI 终局面板（当前展示 `killed/casualties/score`）。注意 DELTA（人口基数最多）与 FOXTROT（人口稀疏但城多）的 perk 会直接影响本项，需与 11.1 的弹数梯度一起复核平衡。
- **关联文件**：`src/sim.js`（`ranking`）、`src/ui.js`（终局计分面板）、`DESIGN.md §6.2`。

### 11.9 [✅ 已落地] 城市列表中军事单位应改为「图例 × 数量」，而非单个中文字

- **现象**：抽屉城市列表里，关联军事单位用「井 / 防 / 雷」单个中文字表示（如 `3井2防1雷`），识别成本高，且与 HUD 图例的图形语言割裂。
- **现状定位**：`ui.updateCities`（`src/ui.js:251-286`）在 `:278` 拼出 `cfText = (ns ? ns+'井':'') + (na ? na+'防':'') + (nr ? nr+'雷':'') || '—'`，三种单位各用一个汉字缩写。图例图形（发射井=六边形 / 防空=三角 / 雷达=椭圆+支杆，见 §3.2）只在抽屉图例区出现，没有复用进城市列表。
- **修订方向**：城市列表的关联设施改用「与 HUD 图例同形的图形 × 数量」呈现（即每个单位类型画一次对应图例小图标 + 数字），与 §3.2「同一套图形」原则一致；敌方无情报时仍显示 `?`。可用内联 SVG（图例已是 SVG，`ui.js` 图例区可复用同一组 path），避免再引图片资源。
- **关联文件**：`src/ui.js`（`updateCities`、图例区 `EFFECT_TEXT`/图例 SVG）、`DESIGN.md §3.2`。

---

### 11.10 [✅ 已落地·补记] 战略核潜艇（此前仅代码注释，未入档）

- **机制**：每阵营 2 艘 × 2 枚 SLBM（`subsPerFaction` / `missilesPerSub`），与发射井同权的机动发射平台：带弹、可发射、发射后同样被弹道溯源暴露；不绑定城市（`cityId = null`），不画连线。
- **落点**：`src/sim.js deployUnits`、`src/data.js CONFIG`。平衡代价记录见 `CONFIG` 内注释（BRAVO `perkSamProbBonus` 0.08 → 0.03 的对冲）。

### 11.11 [✅ 已落地] 海陆掩膜与布阵地理（潜艇上岸 / 发射井落水 / 井贴敌城）

- **现象**（2026-09-10 用户核查 + 实测复现，seed 20260908）：旧深海判据「离最近城市 ≥ 9°」把撒哈拉 / 澳洲内陆 / 中亚当海洋 —— **12 艘潜艇 7 艘爬上了陆**；发射井方位用黄金角随机撒，**ALFA 一口井落在距敌城（铁京）1° 处**，另有一批修进了海里。
- **修订**：
  1. 新增 `src/landmask.js`（1° 分辨率海陆位图，8.1 KB / base64 10.8 KB），由 `tools/gen-landmask.py` 从 Natural Earth 110m 陆地边界（公有领域，`tools/ne_110m_land.geojson`）光栅化产出：3×3 子采样取多数 + 扫描线填充 + 奇偶计数（湖泊自动成水）。改贴图 / 改判据时重跑 `python tools/gen-landmask.py`。
  2. 布阵统一走 `pickAround`（城市环上采样 36 方位选最优）：发射井硬性要求陆上 + 内陆加分 + 「离敌国最近城市尽量远」，窄长国土半径递减重试（10° → 6° → 3.5°）；防空 / 雷达同样硬性要求陆上；潜艇只从「深海四邻皆水」的格点里选。
  3. 潜艇选点口径（用户定稿）：① 尽量远离本土（主项）；② 覆盖远离本土的目标区（离敌城过远有惩罚）；③ 相互间距 ≥ 28°（全局共享，三档放松兜底）。
- **验收**（已入 `tests/headless.js`，20 种子）：潜艇 0 上岸 / 发射井·防空·雷达 0 落水 / 发射井离敌城 ≥ 8°（实测最小 13.2°）/ 潜艇间距 ≥ 28°。
- **关联文件**：`src/sim.js`、`src/landmask.js`、`tools/gen-landmask.py`、`tools/ne_110m_land.geojson`、`index.html`（脚本加载链多一个 `landmask.js`）。

### 11.12 [✅ 已落地] 城市被毁后，关联设施应灰显而非消失

- **现象**：城市被抹除时 `sim` 已把关联设施置 `disabled`，但 `render.syncUnits` 对 disabled 单位直接跳过 —— 战损凭空蒸发，玩家看不到"这里曾有设施"。
- **修订**：失效设施照常渲染，图标与连线染灰（`DISABLED_RGB` 低饱和青灰，与 HUD `--text-dim` 同族）；抽屉城市列表的设施徽章按「在线 N + 失效 M（opacity .35）」分两组显示。雷达覆盖圈对 disabled 雷达仍然隐藏（失效圈没有信息量）。
- **关联文件**：`src/render.js syncUnits`、`src/ui.js updateCities`。

### 11.13 [✅ 已落地] 每回合基础产能，并在统计栏标记

- **需求**（用户）：每回合有基础的规模与核弹数量增加，并在统计栏标记。
- **修订**：`CONFIG.roundPopGrowth = 0.008`（每座存活城市每回合 ×1.008，上限单城 20M，被抹除的城市不复活）；`CONFIG.roundMissileGain = 1`（每回合 +1 枚，按回合数轮转补进未满的发射井，潜艇不补）；`CONFIG.siloExtraCap = 2`（井在初始基数上还能补 2 枚，事件卡 `add_missiles` 同用此 cap）。结算点在 `resolveRound`（`applyEffects` 之前）。
- **标记**：底部统计栏（`#hudLeft .hl2`）常驻 `+0.7/回合`、`+1/回合` 两个小标记，数值来自 `sim.roundGrowthOf`（与结算完全同一套口径，改 cap / 轮转不会两张皮）。
- **平衡影响**（60 局，`tests/balance.js`）：节奏不变（min7 / 中位 11 / max14）；胜率分布 ALFA 22% / BRAVO 40% / CHARLIE 7% / DELTA 2% / ECHO 0% / FOXTROT 30%，与改前同构（BRAVO/FOXTROT 偏高、ECHO 0% 为 §12.3 用户拍板保留项，本轮不调）。
- **关联文件**：`src/data.js CONFIG`、`src/sim.js（applyRoundGrowth / roundGrowthOf）`、`index.html`、`src/ui.js pumpDelta`、`tests/headless.js`。

### 11.14 [✅ 已落地] 抽屉顶部安全区（iframe 场景兜底）

- **现象**：抽屉标题栏写过 `calc(14px + env(safe-area-inset-top))`，但小红书等平台把小工具挂在 **iframe** 里 —— iframe 内 `env()` 恒为 0，等于没留。
- **修订**：抽屉自身标题栏（`#drawer > .dhead`）与关闭按钮改用 `max()` 静态兜底：`padding-top: max(26px, calc(14px + var(--safe-t)))`、`top: max(14px, calc(8px + var(--safe-t)))`。有刘海的宿主吃 env 值，iframe 场景吃静态值；正文内的分组标题不受影响。
- **后续**（§11.18 / §11.19）：`#drawer > .dhead` 这条规则已在 §11.19 被 `#drawer` 的 `padding-top` 取代（标题移入滚动区），避让量的构成不变：静态兜底 26px + `--top-gap` + `--safe-t`。`#drawerClose` 的 `top` 仍按原式。
- **关联文件**：`index.html`。

### 11.15 [✅ 已落地] 顶栏阶段格样式升级

- **需求**（用户）：右侧阶段只有一行 12px 小字，样式单薄。
- **修订**：与 DEFCON 格同构的「小标签（阶段）+ 大字」结构；字号 12 → 14px、加字重与 2px 字距；按阶段着色 —— 态势简报青 / 危机博弈琥珀 / 热核战争红（带红晕）/ 终局灰。格宽 76 → 86px。
- **关联文件**：`index.html`、`src/ui.js updateTop`。

### 11.16 [✅ 已落地] 统计与文案回避「人」的表述

- **需求**（用户）：统计和表述尽量回避「人」这个概念。
- **口径**：人口 → **规模**；伤亡 → **战损**；存活人口 → **存续规模**；「全球伤亡 X.XX 亿人」→「全球战损 X.XX 亿」；事件卡效果说明、终局复盘、战报日志、阵营 perkDesc 同步改写。代码内部字段（`pop` / `casualties` / `killed`）不动，只改呈现层。
- **关联文件**：`index.html`、`src/ui.js`、`src/sim.js`（日志）、`src/data.js`（perkDesc）、`README.md`。

### 11.17 [✅ 已落地] 顶栏阶段格对齐 DEFCON 样式 + 阶段相关的底部统计调整

- **需求**（用户，4 项）：① 顶部阶段样式与左侧 DEFCON 格统一；② 进入博弈阶段后删除底部规模 / 核弹的每回合增加提示；③ 热核阶段军事单位统计文字前加图例；④ 热核阶段上面的核弹统计改为损失统计。
- **修订**：
  1. `#phase` 与 `#defcon` 同构：内容居中、小标签 9px + 2px 字距、大字加粗 + 1px 字距；两格的标签行 / 大字行分别用固定行高 12px / 22px 锁定 —— 整体高度一致、垂直严格对齐，大字字形独立缩放（DEFCON 19px 单数字 / 阶段 15px 四汉字，行高不受字号影响）；仅按阶段保留着色差异（简报青 / 危机琥珀 / 战争红 / 终局灰）。
  2. `#hudLeft .gr`（`+X/回合` 产能标记）仅简报期展示：`pumpDelta` 按 `phase !== 'briefing'` 给 `#hudLeft` 挂 `.nogrowth`（CSS `display:none`）；标记用 `margin-left:-8px` 抵消 `.hl2` 的 10px flex gap，紧贴前面的数字（仅留 2px 缝）。
  3. `#forceStat` 四格（发射井 / 潜艇 / 防空 / 雷达）文字前内联与抽屉图例区同一套 SVG path（11px），图标基线对齐后 `translateY(1px)` 视觉居中。
  4. `#hudLeft .hl2` 的弹头统计在热核阶段（war / over）切换为**战损**（`stats[player].casualties`，M 口径，遵循 §11.16 措辞）；标签由静态「弹头」改为 `#msLab` 动态切换；热核期弹头 Δ 闪烁不再触发（发射条自己会扣数字）。
- **关联文件**：`index.html`、`src/ui.js（init / pumpDelta）`。

### 11.18 [✅ 已落地] 顶部统一留白 `--top-gap`（顶栏整体下移）

- **需求**（用户）：顶部界面留空 50px。
- **修订**：新增 `:root` 常量 `--top-gap: 50px` 作为唯一真源，三处引用它：
  1. `#top` —— `top: calc(var(--top-gap) + var(--safe-t))`，`padding` 里不再承担避让（改为 `0 48px 0`）。**背景与内容一起下移**，上方 50px 是真的空出来露出星球，不是栏内留白。
  2. `#alert` —— `top: calc(96px + var(--top-gap) + var(--safe-t))`。它锚在顶栏下沿 15px 处（顶栏高 81px），顶栏下移多少就得跟多少，否则会被顶栏盖住。
  3. `#drawer > .dhead` / `#drawerClose` —— 在 §11.14 的静态兜底之上再叠加 `--top-gap`，抽屉头与顶栏同一下移量。
- **口径**：这是 §11.14 的延伸 —— iframe 内 `env()` 恒为 0，所以顶部避让不能只靠安全区，需要一条显式常量。改这一个值，三处一起动。
- **关联文件**：`index.html`（`:root` / `#top` / `#alert` / `#drawer`）。

### 11.19 [✅ 已落地] 抽屉「阵营 · 存续规模」不再固定在顶部

- **现象**（用户）：标题固定在抽屉顶部，内容一滚就和下面的东西对不上。
- **病因**：`#drawer > .dhead` 是 `#drawer` 的**直接子级**，落在滚动容器 `#drawer .dbody` **之外** —— `.dbody` 的 `scrollHeight` 是视高的 3 倍，滚过阵营列表后底下已是图例与城市列表，标题却还钉在原处。加上 §11.18 的 `--top-gap`，这个固定头还从 48px 长到了 98px，问题被放大。
- **修订**：
  1. 把 `阵营 · 存续规模` 移进 `#drawer .dbody`，成为第一个分组标题 —— 与「图例 · 军事单位」「城市 · 点选可定位」**同级同款**（同为 `.dhead`、同左缩进 77px、同样随内容滚动）。
  2. 原 `#drawer > .dhead{padding-top: …}` 规则删除，避让量改挂到 `#drawer` 的 `padding-top`（构成不变：26px 静态兜底 + `--top-gap` + `--safe-t`）—— 标题走了，这块空间只剩关闭按钮。
  3. 新增 `#drawer .dbody > .dhead:first-child{margin-top:0}`：分组标题的 14px 上边距对第一个元素是多余的。
  4. `#drawerClose` 加 `border-radius:50%` + `rgba(6,12,18,.92)` 底衬 + `z-index:2`。**标题移入滚动区后内容会滚到按钮底下**，没有底衬的话行文字会穿过 ✕ 变得读不出来。
- **关联文件**：`index.html`（`#drawer` / `#drawerClose` / `.dbody .dhead` / 抽屉 DOM）。

---

## 12. 下阶段开发规划（2026-09-09 制定，截止 2026-09-21，剩 12 天）

### 12.0 当前状态判定（2026-09-09 16:00 更新）

| 维度 | 状态 |
|---|---|
| 功能完整度 | **M1 + M2 已完成**；§11 全 9 项修订全部落地（含 11.6 剩余的辉光脉冲） |
| 无头断言 | `headless.js` 121 / 121 通过，节奏 min7 中位 11 max14 |
| 浏览器冒烟 | `smoke-browser.js` http + file:// 双通道 ✓（渲染层无报错） |
| **平衡** | ✅ **用户决定不调**（2026-09-09 拍板）—— 详见 §12.3 用户决策记录；`balance.js` 主列已换成剩余人口（不再误导调参），但**不增加胜率下限门槛**（ECHO 0% / DELTA 4% 保留作为策略选择的一部分） |
| P0-2 代码缺陷 | ✅ **4 处全部修复**（见 §12.3） |
| P1 体验闭环 | ✅ **全部完成**：11.6 辉光脉冲 + WebAudio 程序化音效（默认关）+ 终局复盘三行 |
| 打包 | ⚠️ `dist/` 仍为空，P2 推迟到用户再启动 |
| 物料 | 截图已有 4 张最新冒烟（`docs/`），笔记 / 封面 / 视频未做 |

### P0-1　修平衡回归：ECHO 0 胜、DELTA 4 胜（最高优先）

**实测**（150 局 `autoPlayer`，seed 1–150，`sim.ranking` 口径）：

| 阵营 | 胜率 | 平均剩余人口 | 平均伤亡 | 平均名次 |
|---|---|---|---|---|
| ALFA | 17% | 43.9M | 47.3M | 3.23 |
| BRAVO | **39%** | 52.7M | 37.9M | 2.32 |
| CHARLIE | 7% | 40.4M | 47.8M | 3.65 |
| DELTA | **4%** | 33.2M | **84.5M** | 4.51 |
| ECHO | **0%** | 27.1M | 60.7M | 5.17 |
| FOXTROT | **33%** | 53.5M | 23.7M | 2.12 |

理想胜率 17%。BRAVO + FOXTROT 两家吃掉 72% 的胜局，ECHO 150 局零胜 —— 选 ECHO 等于认输，
这是**比「某阵营独大」更严重**的问题（后者至少还能玩，前者是直接废掉一个选项）。

**归因（已做对照实验，勿凭直觉判断）**：

| 变量 | ALFA | BRAVO | CHARLIE | DELTA | ECHO | FOXTROT |
|---|---|---|---|---|---|---|
| A 现状（梯度 101 弹 + 剩余人口计分） | 17% | 39% | 7% | 4% | 0% | 33% |
| B 还原「六方统一 18 弹」 | 13% | 31% | 5% | 5% | 2% | **44%** |
| C 关掉人口 perk（DELTA ×1 / FOXTROT ×1） | 11% | **47%** | 6% | 5% | 1% | 31% |
| D 换回旧计分（伤亡差） | 19% | 33% | 15% | **0%** | 1% | 32% |

结论：**§11.1 的核武梯度不是主因，反而略微收窄了差距（B 组 FOXTROT 更极端 44%）**；
§11.8 的计分口径也不是主因（D 组 DELTA 更差 0%）。真正的原因是**地缘 + AI 目标函数的「人口磁铁」**：
AI 打分 `sc = pop / (飞行时间 + 8)^0.10`、`topK=30`，人口权重是线性的，
于是人口最多的 DELTA（开局 113M）成为全员首要靶子，偏远且人口稀疏的 FOXTROT（12 城散在大洋洲）天然挨打最少。

**修订方向（按性价比排序，先扫描再定值）**：

1. **目标函数去磁铁**（首选）：把 `pop` 换成 `Math.pow(pop, 0.5)`，削弱「越大越必打」的线性放大。
   扫描 `aiDistWeight ∈ {0.05, 0.10, 0.20, 0.30}` × `popExp ∈ {0.5, 0.7, 1.0}` × `topK ∈ {20, 30, 40}` 共 36 组 × 150 局。
2. **计分改存活率**（备选，需玩家测试）：排名键从 `popLeft` 换成 `popLeft / pop0`。
   直接消除「初始人口多 = 靶子大」的双重惩罚，让 DELTA 的 population perk 不再是负资产。
   ⚠ 代价是与 §11.8 定下的「谁活下来的人多」直觉口径冲突，终局面板要改成「存活率 %」而非「xx M」。
   **先做方案 1，若仍拉不平再评估方案 2。**
3. **给偏远阵营加成本**（兜底）：FOXTROT 打别人要飞 30–40 s，天然输出慢；
   若方案 1、2 后它仍 >33%，可降低其 SAM 数（4 → 2）或提高被命中杀伤，作为最后手段。

**验收标准**（写进 `tests/balance.js` 的硬门槛，当前脚本只有上限没有下限，掩盖了本问题）：

- 胜率区间 **8% ~ 33%**（即理想 17% 的 0.5× ~ 2×），且**任一阵营 ≥ 5%**
- 伤亡极差 ≤ 3.0x（当前 3.4x）
- 节奏仍满足 min ≥ 7 / 中位 8–14 / max ≤ 14

### P0-2　三处代码缺陷（30 分钟内可清）

| # | 位置 | 问题 | 修法 |
|---|---|---|---|
| 1 | `src/ui.js:88` | `sig.target = '\x00'` —— 源码里嵌了**字面 NUL 字节**（当哨兵值用）。后果：整个文件被 git/grep/diff 判为二进制，`grep` 直接返回 "binary file matches"，压缩器与部分解析器也有风险 | 改成 `sig.target = null`（`''` 会与 `renderTarget` 的初始 key 撞车导致首屏不渲染「未选定目标」，必须用 `null`） |
| 2 | `tests/balance.js:61` | `citiesLost[r.code] += (10 - r.citiesAlive)` 硬编码 10，FOXTROT 有 12 城 → 失城数恒少 2 | 改成 `DC.CITIES_BY_FACTION[r.code].length - r.citiesAlive` |
| 3 | `tests/audit-d1.js:36` | 报告写死「✓ 60 城名称逐字一致」，实际校验 62 城 | 改成 `'✓ ' + DC.CITIES.length + ' 城名称逐字一致'` |

另：`tests/balance.js` 的【平衡】表仍以 `平均得分`（`killed − casualties`）为主列展示，
而 §11.8 后它已不是排序键（DELTA 显示 −54.1 会误导调参）。主列应换成 `平均剩余人口`。

### P1　体验与演出（P0 之后，时间允许才做）

- **11.6 剩余项**：城市受击瞬间辉光脉冲（0.25s ×1.8 回落，配合 bloom 阈值 0.72 只峰值过阈）。
- **音效**（M3 内容，目前完全缺失）：用 WebAudio **程序化合成**，不引入任何音频文件 ——
  小工具红线禁止外部资源，而 `.mp3/.wav` 会显著撑大包体。建议 4 个音：DEFCON 跃迁、导弹发射、拦截爆闪、核爆低频轰鸣。
  必须默认静音 + 提供开关（手机外放场景），且首次交互后才 `resume()` AudioContext（浏览器自动播放策略）。
- **终局复盘**：终局面板目前只有排名表。加 3 行「本局关键节点」（谁先拉满 / 首枚落地 / 你的命中率与拦截损失），
  让「这一局为什么输」有答案 —— 目前玩家只能看到一个数字。
- **情报收益兑现**（§11.2 的遗留半项，关乎核心玩法而非观感，建议优先于音效）：
  `u.exposed` 目前只控制「能不能看见 / 能不能点它反击」，不影响打它的效果。
  §5.2 承诺的「情报战成为博弈期核心取舍」因此只兑现一半 —— 情报目前是「解锁目标」而非「提高质量」。
  建议：对 `exposed` 的敌方目标，命中时杀伤率 ×1.15（或 SAM 对其拦截率 −0.1），
  让「花一个危机回合去做侦察」在数值上有回报，与 `degrade_facility` 形成「侦察 → 定位 → 削弱 → 打击」的完整链条。

### P2　打包与投审（硬截止，不可挪）

- 产出 `dist/` zip：按 `.skill/minitool-zip-builder/` 规范压缩**内容**（`index.html` 在根），
  按 §7.7 建议**不打包 `earth.jpg`**（file:// 下必然 CORS 失败，去掉可省约 450 KB）。
- 逐项过 `zip-artifact-spec.md` §6 与 `performance-budget.md` §6 自检清单。
- 真机冒烟：`tests/smoke-mobile.py` + `tests/smoke-browser.js` 各跑一遍，确认无报错、帧率达标、`file://` 下地球贴图正常。

### P2　小红书物料

- 笔记正文 + 封面图（竖屏 3:4）+ 15–30s 实机录屏（危机博弈一段 + 核爆一段）。
- 录屏需真机/模拟器，模型无法代录用户电脑屏幕，需要用户侧完成。

### 12.1 建议排期（12 天）

| 日期 | 任务 | 产出 |
|---|---|---|
| 9/9（今天） | P0-2 三处缺陷 + balance.js 门槛与展示列改造 | 报告数字可信 |
| 9/10–9/11 | P0-1 参数扫描（36 组 × 150 局）+ 定值 + 复核 | 胜率回到 8%–33% |
| 9/12–9/13 | P1：11.6 脉冲 + 终局复盘面板 | 体验闭环 |
| 9/14–9/15 | P1：WebAudio 音效（静音默认 + 开关） | M3 完成 |
| 9/16–9/17 | P2：打包 + 合规自检 + 真机双冒烟 | `dist/` 可提交 zip |
| 9/18–9/20 | P2：笔记 / 封面 / 录屏 + 缓冲 | 投稿物料 |
| 9/21 | 提交投稿 | — |

> **风险提示**：P0-1 是唯一可能拖期的项 —— 参数扫描需要跑 36 × 150 局，单局约 3000 tick，
> 预计单组 3–8 s、全量 5 分钟内可完成（本地跑，无需优化）。真正的风险是**扫描后仍拉不平**，
> 那就必须启用备选方案 2（计分改存活率），而这需要改终局 UI 文案，会额外吃掉半天。
> 建议 9/11 结束前做一次 go/no-go 判断。

### 12.2 本轮核对顺带发现的「文档—代码」一致性规则

后续改动请遵守，避免再次出现本轮这种「文档说 60 城、代码 62 城」的漂移：

1. 数量型设定（城市数 / 单位数 / 事件卡数 / 各 perk 参数）改完 `src/data.js`，**必须**同步本文件 §2.2 / §3 / §4.2 与 `tests/headless.js` 的硬编码断言。
2. `tests/` 三个脚本各自的数字口径要与 `sim.js` 一致：`balance.js` 读 `ranking`（popLeft）、`audit-d1.js` 读 `DC.CITIES`（含 perk 前基准）—— 报告里要标注清楚读的是哪一层，否则调参会看错数。
3. 任何「文档说 A、代码是 B」的情况，以**代码为准**修文档，不要反过来（代码是跑过测试的）。

### 12.3 本轮（2026-09-09 16:00）完成清单

> 用户当日下达四条指令，按从轻到重逐项落地，本节是它们的最终落点与口径，
> 同时记录两个需要回看的「未做但已讨论」决定。

#### 用户决策记录

1. **平衡不调** —— 上一节 §12 / P0-1 的参数扫描作废。「选阵营本身就是策略的一环」
   意味着 ECHO 0% / DELTA 4% 不视为缺陷，而是玩家应得的风险信号。
   `tests/balance.js` 的【判定】保持现状（**只判上限不判下限**），把主列改成剩余人口仅是
   展示口径修正，不是再启调参的入口。
2. **P2 不做** —— 打包 / 合规自检 / 笔记 / 封面 / 视频 / 软著材料全部推迟到下次启动。
   本轮收在「功能 + 体验」完整可用即可交付的状态，不进小红书提交流程。

#### 完成项（与对应代码 / 测试 / 截图）

| 序 | 任务 | 落点 | 验证 |
|---|---|---|---|
| 1 | **城市定位环** —— 每城光点外描一圈细白环（直径 1.55× 光点），让玩家看清具体坐标 | `src/render.js` 新增 `ringTex()` / `ringPoints` 层 / `syncCities` 同步 / 选中环 `setTarget` / `clearTarget` | 浏览器冒烟截图 `docs/screenshot-d3.png` 可见（环清晰、不与光点阵营色冲突） |
| 2 | **§11.6 剩余辉光脉冲** —— 受击瞬间城市亮度 ×2.8 上冲、0.25s 回落，过 bloom 阈值只在这一瞬炸光 | `src/render.js` `cityPulse` + `syncCities(state, dt)` | 视觉验证（需用户开浏览器看） |
| 3 | **WebAudio 程序化音效** —— 5 个音（DEFCON 跃迁 / 发射 / 拦截 / 核爆 / 拒），全部合成、不引音频文件，默认静音，顶栏开关 | `src/audio.js` 新增、`src/ui.js` 触发点（DC 变化 / impacts / 拦截累计 / fire / deny）、`index.html` 顶栏 `#sndBtn` | 无头断言里加入 `audio.js` 红线扫描；浏览器冒烟不报错即视为通过 |
| 4 | **终局复盘三行** —— 导火索 / 首枚落地 / 你的战果 | `src/sim.js` 加 `firstImpact` + `stats.hits/lost`；`src/ui.js` `buildRecap`；`index.html` 加 `#ovRecap` 与 CSS | `tests/headless.js` 加 3 条断言（hits 枚数口径、firstImpact 完整性、lost 与 intercepts 恒等） |
| 5 | **P0-2 / 代码缺陷 4 处** —— 见下表 | 见下表 | 全部由 `headless.js` / `audit-d1.js` / `balance.js` 报告佐证 |

| 缺陷 | 位置 | 修法 |
|---|---|---|
| ui.js 第 88 行 `sig.target = ' '` —— 源码字面 NUL，导致整个文件被 git/grep 视为二进制 | `src/ui.js:88` 与 `sig` 声明 `target: ''` | 改为 `null`；`renderTarget` 的 `if (key === sig.target) return` 比较改为 `'' !== null`，首屏「未选定目标」正常触发 |
| `tests/balance.js:61` 失城数硬编码 `10 - citiesAlive`，FOXTROT 12 城恒少 2 | `tests/balance.js` 61 行 | 改为 `DC.CITIES_BY_FACTION[r.code].length - r.citiesAlive` |
| `tests/audit-d1.js:36` 报告写死「✓ 60 城名称逐字一致」 | `tests/audit-d1.js` 36 行 | 改为 `'✓ ' + DC.CITIES.length + ' 城名称逐字一致'` |
| `tests/balance.js`【平衡】主列展示已不是排序键的「平均得分」，DELTA 显示 −54.1 会误导调参 | `tests/balance.js` 第 80-89 行 | 主列换成「平均剩余人口」，去掉 score 列（`tests/headless.js` 与 `tests/audit-d1.js` 不受影响） |

#### 顺带改进（未要求但顺手清掉的脏）

- `tests/smoke-browser.js` 把 playwright 绝对路径从硬编码改为 `process.env.DC_PLAYWRIGHT || 默认`，
  符合工作区「禁止硬编码绝对路径」的约定（其他机器也能跑）。
- `tests/smoke-browser.js` 的「竖屏横向溢出」探针改为先摘 `display:none` 再量，
  排除抽屉（设计上 `transform:translateX(100%)` 移到屏幕外）造成的 328px 假警报。
- `tests/smoke-browser.js` 的城市行数判定改为读 `DC.CITIES.length`，弹头数判定改为
  `w.ammo > 0`（危机博弈期的补弹卡会让开局弹头 ≠ 满编，硬等必误报）。

#### 当前测试栈

- `node tests/headless.js` → 121 / 121 ✓
- `node tests/balance.js 60` → 节奏 ✓ / 平衡按用户决策保留极差分布
- `node tests/audit-d1.js` → ✓ 62 城名称逐字一致
- `DC_PLAYWRIGHT="<你的路径>" node tests/smoke-browser.js` → http + file:// 双通道 ✓
